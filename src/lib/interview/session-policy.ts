/** Invitations are single-use entry tickets; browser access is separately bounded. */
export const INTERVIEW_INVITATION_TTL_SECONDS = 24 * 60 * 60;
export const INTERVIEW_SESSION_TTL_SECONDS = 30 * 24 * 60 * 60;

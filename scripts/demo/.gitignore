# Command Center Demo Pack — runtime data must NEVER be committed.
#
# .runtime/ holds the seeded SQLite DBs (interview + dashboard), the per-profile
# OpenClaw workspaces, and the rotated cookie-secret.txt. It is (re)generated by
# reset-demo.sh at seed time and is disposable — treat it as ephemeral state.
.runtime/

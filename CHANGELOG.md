## [v5.1.1] — 2026-07-10 — deploy(cc): reconcile v5.1.0 write-back-auth fix onto the operator box + preserve the welcome-cards home

Deploys the v5.1.0 task-API write-back-auth fix (entry below) to a box whose live build (local v4.73.0 `6ab73df`) predated the fix. That build dispatched dept/main agents with NO `Authorization` header on their write-backs, so every write-back 401'd against `src/middleware.ts` Gate B and finished tasks froze `in_progress` until the stuck-in-progress sweep moved them to `blocked` (observed: 94 blocked / 39 backlog / 0 in_progress). Reconciled by cherry-picking the seven-welcome-cards home (`6ab73df`) on top of tag `v5.1.0` (`f200192`) so this build carries BOTH the write-back-auth backend fix AND the seven-card `/` home + `/overview` grid.

### deploy — v5.1.0 backend fix + welcome-cards home (this box)
- All v5.1.0 backend fixes intact: `src/lib/mc-auth.ts`, `src/lib/jobs/finished-work-recovery.ts`, `src/lib/jobs/execution-watcher.ts` (dept-namespace probe), `src/lib/jobs/stale-task-sweep.ts` + `stuck-in-progress-sweep.ts` disk-recovery, `scripts/recover-blocked-deliverables.ts`, `ecosystem.config.cjs` MC_API_TOKEN/WEBHOOK_SECRET passthrough.
- `src/app/page.tsx` = the seven welcome cards (verbatim `f7a5098`); `src/app/overview/page.tsx` = the v4.66.0 overview grid at `/overview`; `src/app/tasks/all/page.tsx` unchanged.
- Version bumped 5.1.0 → 5.1.1 across `version`, `package.json`, `package-lock.json`. Manual atomic swap deploy (NOT `scripts/atomic-deploy.sh`, which would pm2-delete the sibling demo apps).

## [v5.1.0] — 2026-07-10 — fix(cc-writeback): task-API write-back auth + fail-loud + trapped-work recovery

Closes the fleet-wide "carded-but-trapped" 401 defect. Department/main agents finished work but their write-backs (`POST /api/tasks/:id/activities`, `/deliverables`, `PATCH /api/tasks/:id`) carried no `Authorization` header, so the fail-closed middleware 401'd them and finished tasks froze `in_progress` until a sweep moved them to `blocked`/`backlog` — throwing the finished deck/site/asset away.

### fix — canonical write-back auth (`src/lib/mc-auth.ts`, NEW)
- The ONE place that builds `Authorization: Bearer $MC_API_TOKEN` write-back headers (`missionControlAuthHeaders`), the fail-loud dispatch preflight (`checkTaskWriteAuth`), `MissionControlWriteError`, and the shared write-back instruction block (`renderWriteBackInstructions`). Never logs/returns the token value.
- `src/app/api/tasks/[id]/dispatch/route.ts` + `src/lib/task-dispatcher.ts`: both dispatch paths render the ONE shared write-back block (bearer on all three calls) and run the fail-loud preflight — a task is HELD with a SYSTEM report instead of dispatched into a silent-401 trap when `MC_API_TOKEN` is unprovisioned.
- `src/lib/orchestration.ts`: server-side write-back helpers attach the bearer and throw `MissionControlWriteError` on 401/403 (fail loud) instead of the old silent `console.error` swallow.

### fix — execution-watcher namespace (`src/lib/jobs/execution-watcher.ts`)
- The late-completion reconcile probed only `agent:main:`, missing every dept agent's `TASK_COMPLETE:` (dept sessions live at `agent:dept-<slug>:`). It now resolves the dept session key exactly as dispatch does (`resolveSpecialistSessionKey`) and probes it FIRST, then falls back to `agent:main:`, so a late dept completion is found and advanced to `review` instead of swept to `blocked`.

### fix — don't discard finished work (`src/lib/jobs/finished-work-recovery.ts`, NEW)
- Shared recovery gate used by BOTH sweeps: on a stalled `in_progress` task it checks for a registered deliverable OR on-disk output (artifact / manual-dispatch dir) and recovers the card to `review` (redelivering on-disk output) instead of blocking/bouncing it.
- `stuck-in-progress-sweep.ts` refactored onto the shared gate (behavior identical). `stale-task-sweep.ts` gains the same disk-recovery for `in_progress` before returning to backlog (the second demotion path that previously discarded work). `scheduler.ts` sweep logs surface recovered counts.

### recovery — one-time blocked-pile sweep (`scripts/recover-blocked-deliverables.ts`, NEW)
- DRY-RUN by default (read-only, zero writes): classifies every `blocked` card (ONBOARDING / TEST / DEMO / SEED / DUPLICATE / REAL) and proposes a per-card action; only evidenced REAL cards recover to `review`, never a blanket redeliver. APPLY is double-guarded (`--apply --yes` + `MC_API_TOKEN`) and dogfoods the write-back-auth fix via the HTTP API + bearer. Run APPLY only after this release is live on the box.

### hardening + bundled
- `ecosystem.config.cjs`: pass `MC_API_TOKEN` + `WEBHOOK_SECRET` through the PM2 env (conditional spread) so a cwd-drift restart can't silently drop them.
- `src/lib/jobs/weekly-done-clear.ts`: soft-archive orphan EMPTY cards (strict criteria never touch finished/trapped work).
- `src/lib/routing/departments.config.ts`: drop bare `pitch` from the Presentations keywords (misroute; `pitch deck` / `investor deck` still route here).

### tests
- New: `mc-auth`, `task-writeback-fail-loud`, `stuck-in-progress-recover`, `stale-task-recover`, `execution-watcher-namespace`, `recover-blocked-deliverables`, `writeback-pipeline-e2e` (26 tests, all green; includes negative 401 + fail-loud preflight + end-to-end recovery). `npm run test:unit` net-new failures = 0 vs baseline; `tsc --noEmit` clean.

## [v5.0.0] — 2026-07-10 — chore(release): v5.0.0 — major-version milestone (Anthology producer Command Center)

Major-version milestone marker atop v4.73.0. **No functional change from v4.73.0** — the code tree is byte-identical; this release only advances the version number (and this CHANGELOG entry) to mark the 5.x line. Nothing is added, removed, or altered in application behavior.

### What v5.0.0 cumulatively represents
The 5.0.0 line rolls up the Anthology producer Command Center delivered across the 4.7x series, on top of the same task/board surface the fleet has run all along:
- **Session-gated board door** (`src/app/api/anthology/gate/route.ts`, `src/app/participant/_lib/gate-engine.ts`) — POST/GET `/api/anthology/gate` with the pure `gate-engine` decision logic (v4.73.0).
- **Producer card face + 3-zone Gate Panel** (`src/components/anthology/AnthologyCardFace.tsx`, `GatePanel.tsx`, `anthology-card.ts`, `gate-actions.ts`) — hold / exclude / finalize gate decisions; order fields relayed only for the finalize-action set (v4.73.0).
- **Assembly cockpit** (`src/components/anthology/AssemblyCockpit.tsx`, `assembly-cockpit-logic.ts`, `finalize-action.ts`) — readiness → arm → order → sign-off, relaying the whole finalize-action order payload to the board (v4.73.0).
- **Board hygiene** (`scripts/u14-anthology-board-hygiene.py`) — reversible archive + producer Welcome board hygiene (v4.73.0).
- **Additive relay wiring** (`src/app/api/tasks/ingest/route.ts`, `src/components/MissionQueue.tsx`, `src/components/TaskModal.tsx`) — assembly passthrough (`assembly_state` + readiness + ordering, incl. the `cockpit_view` alias), `confirm_order`, and `anthology_id` relayed verbatim; double-render fixed. A plain (non-anthology) participant status behaves byte-identically to v4.72.0 (v4.73.0).
- **Recent board/middleware fixes carried in** — the v4.72.0 middleware fix (board loads its own data on plain-tunnel boxes without weakening ingest/external auth) and the additive board relay wiring above.

### Verification
- Version bump only: all 5 version-consistency locations (`/version`, `package.json`, `package-lock.json` root + `packages['']`, CHANGELOG top entry) set to v5.0.0 via `scripts/bump-version.sh`. Annotated tag `v5.0.0` cut for the release. No source files changed vs. v4.73.0.

## [v4.73.0] — 2026-07-10 — feat(anthology-cc): producer Command Center — session-gated board door, producer card + 3-zone gate panel, assembly cockpit, board hygiene

Lands the Anthology producer Command Center as a forward-merge onto v4.72.0 (`integration/anthology-cc`, 10 commits, base `afb9481`). Four feature bundles ship together behind the anthology gate; the existing task/board surface is untouched except for additive relay wiring, so a plain (non-anthology) participant status behaves byte-identically to v4.72.0.

### feat — session-gated board door (`src/app/api/anthology/gate/route.ts`, `src/app/participant/_lib/gate-engine.ts`)
- B10/U11: POST/GET `/api/anthology/gate` — a session-gated door for the producer board, with the pure `gate-engine` decision logic unit-tested standalone.

### feat — producer card face + 3-zone Gate Panel (`src/components/anthology/AnthologyCardFace.tsx`, `GatePanel.tsx`, `anthology-card.ts`, `gate-actions.ts`)
- B11/U12: producer card face + a 3-zone Gate Panel driving hold / exclude / finalize gate decisions. Order fields are ignored for non-finalize (hold/exclude) decisions and only relayed for the finalize-action set.

### feat — assembly cockpit (`src/components/anthology/AssemblyCockpit.tsx`, `assembly-cockpit-logic.ts`, `finalize-action.ts`)
- B12/U13: readiness → arm → order → sign-off cockpit; the finalize action relays the whole finalize-action order payload through to the board.

### feat — board hygiene (`scripts/u14-anthology-board-hygiene.py`)
- B13/U14: reversible archive + producer Welcome board hygiene.

### wiring — additive relay (`src/app/api/tasks/ingest/route.ts`, `src/components/MissionQueue.tsx`, `src/components/TaskModal.tsx`)
- Relay the assembly passthrough (`assembly_state` + readiness + ordering, also accepting the U9 `cockpit_view` alias), `confirm_order`, and `anthology_id` verbatim through the ingest/board-status path — never stripped; double-render fixed. A plain participant status carries no assembly passthrough keys.

### tests — new anthology suites (`tests/unit/anthology-gate-route.test.ts`, `anthology-gate-panel.test.ts`, `anthology-assembly-cockpit.test.ts`)
- 88 cases across the three suites covering the gate route/relay, gate-panel actions, and assembly-cockpit logic.

### Verification (observed on the merged tree at the `--no-ff` merge commit, node v22.19.0)
- `npx tsc --noEmit`: exit 0 — typecheck clean across all new anthology modules.
- `npm run build` (`next build`): exit 0 — Compiled successfully.
- `node --import tsx --test` on the 3 anthology suites: exit 0 — 88 tests, 88 pass, 0 fail.
- Forward-merge onto `origin/main` (`afb9481`) was a clean `--no-ff` — origin/main is a strict ancestor of the branch (0 divergent commits, 10 ahead), no conflict markers. All 5 version locations agree at v4.73.0; annotated tag `v4.73.0` cut at the release (bump) commit. Repo stays fleet-wide — the client name was scrubbed from the anthology fixture in the final integration commit.

## [v4.73.0] — 2026-07-10 — feat(home): restore the seven welcome-cards home at `/`; overview grid stays at /overview, task board at /tasks/all

Restores the operator's original landing page. `/` (`src/app/page.tsx`) is once again the seven-card **"Welcome / Choose where you want to go"** grid — the `'use client'` framer-motion `EntryCard` page — with the seven gradient cards: **View All Tasks** (→ `/tasks/all`), **Departments** (→ `/tasks/by-department`), **Performance Board** (→ `/ceo-board`), **Conversational AI** (→ `/conversational-ai`), **Intelligence Settings** (→ `/settings/intelligence`), **Operator Console** (→ `/operator`), and **Company Settings** (→ `/settings/company`). This **reverses BOTH** the v4.66.0 grid-home redesign AND the v4.72.1 `redirect('/tasks/all')`: `/` no longer redirects. The page is restored **verbatim** from commit `f7a5098` (the last cards version, 288 lines).

Nothing was removed. The operations OVERVIEW grid (the v4.66.0 redesign — KPI strip, department pulse, view tiles, activity feed) stays UNCHANGED and reachable at `/overview` (`src/app/overview/page.tsx`); the cross-department MissionQueue task board stays at `/tasks/all` (the "View All Tasks" card destination). The interview-mode shell lock (`src/middleware.ts`) is untouched and still runs first, so an unprovisioned box is still gated to `/interview` before `/` renders.

### changed — `src/app/page.tsx`
- Replaced the `export default function Home() { redirect('/tasks/all'); }` server component with the 288-line welcome-cards client page, restored verbatim from `f7a5098`. `/` now renders the seven gradient `EntryCard`s instead of 307-ing to the board.

### preserved (explicitly kept, untouched)
- `src/app/overview/page.tsx` — the v4.66.0 operations overview grid, still served at `/overview`.
- `src/app/tasks/all/page.tsx` — the cross-department task board, still the "View All Tasks" destination.
- `src/middleware.ts` — interview shell-lock unchanged.

### Verification (observed on the live box, node v22.19.0)
- `npx tsc --noEmit`: exit 0 — the restored file type-checks clean against current deps (framer-motion ^12.38.0, lucide-react ^0.468.0, date-fns ^4.1.0; hooks `useLogoUrl`/`useCompanyBrand` and `Breadcrumb` all present in the tree).
- Deployed manually, NOT via `scripts/atomic-deploy.sh`, for two box-specific reasons: (1) atomic-deploy Phase 1d `pm2 delete`s every pm2 app whose name contains `blackceo`/`command-center`/`mission-control` that is not the `--pm2-app` — which on this box would destroy the sibling `blackceo-cc-demo-interview` / `blackceo-cc-demo-dashboard` class-demo apps; and (2) Next 14.2 here does not honor its `NEXT_DIST_DIR` out-of-place build mechanism. Manual sequence: snapshot live `.next` → `.next.rollback` (0cvrc7y, rollback armed), `next build` (exit 0, v4.73.0), then `pm2 restart cc-prod` onto the fresh coherent build. Rollback path preserved: `mv .next .next.failed && mv .next.rollback .next && pm2 restart cc-prod`.
- Live authenticated (interview-gate cookie) checks after restart: GET `/` → 200 containing "Choose where you want to go" + "View All Tasks" (7 gradient cards render, no redirect); GET `/tasks/all` → 200 ("Backlog"); GET `/overview` → 200. New `.next/BUILD_ID` `jRjpLM5DG4d5vqOB5m2-U` (was `0cvrc7y_EN1tybe_QiYgf`) served by a freshly-restarted pm2 `cc-prod`.

## [v4.72.1] — 2026-07-10 — feat(home): `/` redirects to the /tasks/all task board; overview preserved at /overview

The operator's main screen now lands directly on his task cards. `/` (`src/app/page.tsx`) is now a minimal server-side `redirect('/tasks/all')` to the cross-department MissionQueue board. The prior operations OVERVIEW grid — the v4.66.0 redesign (KPI strip, department pulse, view tiles, recent-activity feed) — is preserved UNCHANGED at the new route `/overview` (`src/app/overview/page.tsx`), which contains the former HomePage client component verbatim ('use client', all fetches and logic identical). `/kanban`'s existing 308→/tasks/all redirect is untouched.

Non-destructive Path 2: no feature was removed or downgraded. The interview-mode shell lock (`src/middleware.ts`) still runs first, so an unprovisioned box is gated to `/interview` before the `/`→`/tasks/all` redirect is reached; a provisioned box (interview complete) lands on the board.

### changed — `src/app/page.tsx`
- Replaced the HomePage overview component with `export default function Home() { redirect('/tasks/all'); }` (`next/navigation` redirect). Server component; 307 to the board on a page GET.

### added — `src/app/overview/page.tsx`
- The former overview home, moved verbatim (the `HomePage` client component). Reachable at `/overview` and still linked from the board's breadcrumb/nav.

## [v4.72.0] — 2026-07-09 — fix(middleware): board loads its own data on plain-tunnel boxes without weakening ingest/external auth

Fixes a board-blanking regression the operator-box test caught: on any box NOT fronted by Cloudflare Access (a plain Cloudflare Tunnel), v4.71.0's middleware returned 401 on every Command Center data read (`/api/tasks`, `/api/workspaces`, `/api/departments`, `/api/company-health`, `/api/persona-matrix`, `/api/org-chart`), so the board showed 0 tasks / "HTTP 401" / "Unauthorized" = data-blank. The board's browser fetches carry NO bearer token (MC_API_TOKEN is server-only, never exposed to the client — see `src/components/WorkspaceDashboard.tsx`), so they relied on the same-origin passthrough; v4.71.0's security train (DATA-10) hardened that passthrough to require a VERIFIED CF-Access assertion AND flipped `REQUIRE_CF_ACCESS` default-ON in production, neither of which a plain-tunnel box has. This restores v4.69.1's same-origin trust for the board's OWN data reads/writes WITHOUT re-opening what the train closed: the external ingest/webhook write surface (DATA-09/10/11) still requires its MC_API_TOKEN bearer + WEBHOOK_SECRET HMAC.

### fix — same-origin passthrough restored + scoped (`src/middleware.ts`)
- **Root cause #1 — `REQUIRE_CF_ACCESS` default (was `NODE_ENV==='production' ? !== 'false' : === 'true'`).** Reverted to opt-in on ALL boxes (`=== 'true'`, v4.69.1 behavior). Default-ON hard-401s every non-health route (page shell + all `/api` reads) on a box the CF edge does not inject `cf-access-jwt-assertion` for. The fleet does not guarantee CF Access on every one of the ~32 client subdomains, so default-ON blanked the board. External-API security is UNCHANGED: it is always gated by the MC_API_TOKEN bearer (Gate B) and WEBHOOK_SECRET HMAC (Gate A), independent of this flag. An operator who genuinely runs CF Access can still set `REQUIRE_CF_ACCESS=true` for the extra edge gate (which also closes the same-origin READ residual).
- **Root cause #2 — same-origin passthrough gate (was `if (cfJwt && cfEmail && isSameOriginRequest(request))`).** Dropped the `cfJwt && cfEmail` requirement, restoring trust for same-origin browser requests, but SCOPED it to exclude the webhook/ingest family via `if (!isWebhookSecretRoute(pathname) && isSameOriginRequest(request))`. `/api/tasks/ingest`, `/api/webhooks/*`, and `/api/tasks/{id}/status` are NEVER same-origin-passable — they always require the bearer here (plus their route-level HMAC), so a forged same-origin `Origin`/`Referer` cannot reach the write surface. `x-operator-email` is now set conditionally (cfEmail may be absent on a plain-tunnel box).
- Boot-time `REQUIRE_CF_ACCESS`-off log downgraded from a production `[SECURITY ERROR]` to an informational `[SECURITY]` notice, since OFF is now the documented default.
- **Residual (accepted, = v4.69.1):** `Origin`/`Referer` are client-settable, so a non-browser caller reaching the origin directly can forge a same-origin header to READ the board's own data. That is the board's READ surface only; the ingest/write family stays fully auth-gated, and CF Access (`REQUIRE_CF_ACCESS=true`) closes even this residual at the edge.

### docs
- `docs/CLOUDFLARE_ACCESS_SETUP.md` — corrected the two "production MUST keep it true" / "NEVER set false in production" lines to the new opt-in posture (default OFF; set true only on a box actually fronted by CF Access).
- `.github/workflows/qc-cc.yml` — corrected the now-inaccurate "DEFAULT-ON in production" comment on the health-probe fixture's `REQUIRE_CF_ACCESS: "false"` (still pinned false as belt-and-suspenders).

### tests — `tests/unit/middleware-same-origin-board.test.ts` (new, vitest-gated in CI)
- 23-case auth matrix re-importing the middleware per env scenario. Proves, on a plain-tunnel production box: all 6 board READ APIs pass through (200) with NO CF assertion and NO bearer; same-origin board WRITES pass through; external cross-origin without bearer → 401; valid bearer → 200; wrong bearer → 401; and all 5 ingest/webhook routes from a FORGED same-origin caller → 401 (no regression), valid-bearer ingest passes the middleware layer. Also proves: an unprovisioned box still renders the board same-origin while external → 503 and ingest → 503 (fail-closed); and `REQUIRE_CF_ACCESS=true` still enforces Layer 1 (no assertion → 401, CF-verified same-origin read → 200, ingest without bearer → 401 even behind CF). Wired into the vitest `include` (runs in the CI `deep-health-truth-table` job) and excluded from the `npm run test:unit` node:test glob.

### Verification (observed on a fresh clone, node v22.19.0)
- `npm run test:vitest`: exit 0 — 5 files, 123 tests PASS (the 23 new middleware cases + B.1 deep-health truth-table 65 + cc-probe-pm2 8 + floor-department-invariant 5 + seam-parity). This is the CI `deep-health-truth-table` job.
- `npm run build` (`next build`): exit 0 — "Compiled successfully"; TypeScript typecheck clean across the middleware change.
- `scripts/qc-cc.sh`: exit 0 — 133 checks green, 4 environment-only warnings (same set as v4.71.0).
- Duck pipeline e2e (`tests/e2e/duck-test.ts`, mock generator, `next start`): 20/20 subtests PASS — ingest still authenticates with real Bearer + HMAC, unchanged by this fix.
- The full CI matrix (Duck pipeline e2e, B.1 deep-health truth-table + thin-probe fixture, QC Command Center, version-consistency, config-guard, and the rest) was watched to green on the `main` push for this release.
- No new npm dependencies; repo stays fleet-wide (no client names/secrets in the diff); all 5 version locations agree at v4.72.0; annotated tag `v4.72.0` cut at the release commit.

## [v4.71.0] — 2026-07-09 — feat(persona-blend): consume the matcher persona-bundle SUPERSET + audience-confirm write gate (migration 090)

Teaches the Command Center to consume the Skill-23 matcher's persona-bundle SUPERSET — a VOICE decision (an audience persona to write IN blended with a topic persona whose expertise is carried), an operator-confirmed audience, a doer-facing blend directive, and the up-to-10 task-personas plan. Every field is optional at the wire level, so a legacy single-persona result (a non-content task or an older matcher) parses to a null bundle and the CC behaves EXACTLY as before — backward compatible, no regression. A persona is a CRAFT LENS, never an identity: the blend directive ALWAYS carries a non-removable STYLE-INSPIRED-NOT-IMPERSONATION guardrail, injected at parse, persist, AND render so no upstream can strip it.

Merged from `persona-blend/cc` (branch QC 8.6). Rebased onto main after the CI-fix landed; the persona-blend migration was renumbered **089 → 090** because main's security fix (INGEST-10, `add_tasks_source_column`) had already claimed 089. Both migrations are additive and coexist; the module-load duplicate-id guard stays green and the fresh-DB `schema.ts` CREATE mirrors both column sets.

### feat — persona-bundle parse + persist (`src/lib/persona-selector.ts`)
- `parsePersonaBundle()` normalizes a raw selector result into a typed `PersonaBundle`, or NULL when it carries no SUPERSET fields (legacy → CC unchanged). Defensive per-field normalization; `task_personas` hard-capped at 10 (mirrors the matcher contract). The mandatory guardrail is ALWAYS injected into `blend_directive` here — even if the matcher omitted it — so the CC is the last non-bypassable line of defense.
- `persistPersonaBundle()` writes one `task_persona_bundle` row (full JSON + catalog schemaVersion + audience confirm state) and mirrors the resolved VOICE decision + confirmed audience onto `tasks.*`. Idempotent per task (`task_id` UNIQUE → upsert). `tasks.persona_id/name/mode` stay the back-compat mirror of the resolved VOICE persona.

### feat — mandatory guardrail + blend rendering (`src/lib/persona-dispatch.ts`)
- `STYLE_INSPIRED_GUARDRAIL` constant + `ensureBlendGuardrail()` (idempotent injector) + `renderBlendDirective()` (fail-closed: renders the guardrail even for a null/empty directive). `buildPersonaBlock()` now appends the blend directive ON TOP of whichever persona branch fired; a no-blend task renders byte-identically to the pre-blend output.

### feat — audience-confirm write gate (`src/lib/task-dispatcher.ts`, `src/lib/tasks.ts`)
- `evaluateAudienceConfirmGate()` (pure, unit-testable) is called in `autoDispatchTask()` BEFORE the in_progress CAS write: `pending` within deadline → HOLD (write gated); `confirmed`/`not_required`/no-bundle → proceed; `pending` past deadline → NEVER-NAKED release under house-voice governance ONLY (audience never fabricated, `confirm_required` kept visible).
- `holdForAudienceConfirm()` defers the task on a quiet poll window (NOT counted toward the anti-furnace block cap) and surfaces the operator ONCE via `notifySystem` (operator-facing — MOVE-IN-SILENCE, never client spam). `markAudienceDeadlineFallback()` flips state once on the transition; `confirmTaskAudience()` flips to `confirmed` and mirrors `audience_source='operator_confirmed'`.

### db — migration 090 (`src/lib/db/migrations.ts`, `schema.ts`, `types.ts`)
- Additive `tasks` mirror columns (`voice_persona_id`, `topic_persona_id`, `audience_id`, `audience_label`, `audience_source`, `voice_collapsed`, `blend_directive`) + new `task_persona_bundle(task_id UNIQUE → tasks ON DELETE CASCADE, bundle_json, catalog_version, confirm_state, created_at)`. Idempotent, PRAGMA-guarded, mirrored in the fresh-DB `schema.ts` CREATE. `types.ts` gains the `PersonaBundle`/`ResolvedAudience`/`BundleVoiceDecision`/`BundleTaskPersona`/`TaskPersonaBundleRow` interfaces and the `Task` mirror fields.

### enforcement + tests
- **`scripts/qc-cc.sh`** (section 14, migration 090) — 11 drift sentinels: migration additive, schema/types parity, guardrail defined + non-removable, gate-before-write ordering, operator-not-client surface, behavioral proof present.
- **`tests/unit/persona-blend-audience-confirm.test.ts`** (new) + extensions to `fdn3-persona-dispatch-injection`, `prd-1.6-async-persona-selection`, `dep5-persona-plan-multi`, `point10-persona-exhaustion-fallback` — 21 persona-blend cases covering the migration shape, persist/upsert/mirror, the pure gate decision (hold/confirm/deadline), non-removable guardrail, and backward-compat null-bundle.

### Verification (observed on this integration branch, node v22.19.0)
- `npm run build` (`next build`): exit 0 — TypeScript typecheck clean across all new types + persona-blend code.
- `npm run lint`: exit 0 — pre-existing component warnings only; none in any persona-blend file.
- `scripts/qc-cc.sh`: exit 0 — 133 checks green, 4 environment-only warnings; all section-14 checks green under migration 090.
- `npm run test:unit`: all 21 persona-blend cases PASS. The suite also shows ~13 residual failures in UNRELATED modules (interview-detection, runQCOnReview/duck, studio-registry-seed, model-resolution, sentinel-persona, skill-6 card) — reproduced identically on a clean `origin/main` (88abb10) worktree and, for `studio-registry-seed`, identically in isolation on both trees. These are the operator box's environment (real `~/clawd/zero-human-company/testco` filesystem signals + absent KIE/OPENAI/FISH_AUDIO provider env), NOT this change; CI runs hermetically and reported main all-green.
- Migration ids monotonic 085→090, unique (module-load duplicate-id guard passes); no conflict markers; repo stays fleet-wide (no client names/secrets in the diff); no new npm dependencies. All version locations agree at v4.71.0; annotated tag cut before merge.

## [v4.70.0] — 2026-07-08 — fix(floor): board displays EXACTLY the chosen manifest − opt-outs (four seed/board bugs)

Enforces a guaranteed match between a client's CHOSEN departments (departments.json manifest), the PROVISIONED workspaces, and what the Command Center Kanban DISPLAYS — honoring explicit opt-outs. Diagnosed live on a client box whose manifest listed 43 chosen departments but whose board rendered only 42/40, traced to FOUR bugs in this shared repo (so every client was exposed). The invariant, now enforced at the seed AND the board query: for the active company, displayed departments == chosen manifest − explicitly-opted-out.

### fix #1 — department seeding runs every boot/converge (was first-boot-only)
- **`src/lib/db/migrations.ts`** — `autoSeedFromDepartmentsJson()` early-returned the moment the workspaces table had any row, so a manifest that GREW after first boot never re-synced (stranded departments). It now delegates to `reseedWorkspacesFromConfig()` — the same idempotent, additive UPSERT the converge endpoint uses — so boot and converge enforce the invariant identically. Errors are swallowed on the boot path (never crashes startup) and re-thrown on converge (fails loud), preserving prior semantics.
- **`reseedWorkspacesFromConfig()`** is now the single seeder: idempotent UPSERT keyed on dept id, NON-DESTRUCTIVE (never deletes a workspace or any task/agent data), re-homes each chosen dept's `company_id` to the resolved active company (mirrors `scripts/sync-departments-from-build-state.py`), fails closed on a template/partial company-config, and skips explicitly opted-out departments.
- New exported `isDepartmentOptedOut()` predicate recognises every explicit opt-out spelling (`optOut`/`opted_out`/`enabled:false`/`active:false`/`status: opted-out|declined|disabled|inactive`).

### fix #2 — App Development keeps its own lane (removed destructive alias)
- **`src/lib/routing/canonical-slug.ts`** — removed the `'app-development' → 'engineering'` ALIAS_MAP entry. The seed dedup (`findCanonicalWorkspaceId`) skipped a dept whose canonical slug collided, so a client who chose BOTH App Development and Engineering only ever got one lane — even though `departments.config.ts` defines `app-development` as a distinct department and has no `engineering` entry. App Development and Engineering are now distinct canonical slugs; the `software-development`/`software-dev`/`apps` → engineering aliases are unchanged.
- **`tests/unit/cc-board-dedup-reaper.test.ts`** — the dedup test now asserts the two survive as DISTINCT lanes (was asserting the collapse).

### fix #3 — company attribution fails closed on the unpopulated template
- **`src/lib/db/branding-seed.ts`** — `seedCompanyGuarded()` treated the repo template `companyName:"Your Company"` (enforced by `.github/workflows/config-guard.yml`) as a valid brand and seeded a bogus `your-company` company, so departments seeded afterward landed under the wrong `company_id`. New `isTemplateCompanyName()` treats the template sentinels as partial-config (fail-closed): nothing is written and the box is flagged misconfigured rather than mis-attributed. A real brand that merely contains the word "Company" still seeds.

### fix #4 — board query scoped to the active company (no foreign leakage)
- **`src/app/api/workspaces/route.ts`** — the GET board query (both the stats and plain branches — the single choke point every dashboard fetches) had no company filter, so a foreign company's workspace rows leaked onto the board. It now scopes to the active client company; the `default`/NULL sentinel (the box's own unattributed rows, single-tenant) is kept so the board never goes blank while other companies are excluded.
- **`src/lib/company.ts`** — new shared `resolveActiveCompanyId()` (mirrors the `/api/company` pickCompany heuristic) used by BOTH the seed attribution and the board filter so the two always agree.

### enforcement
- **`scripts/qc-cc.sh`** (section 13) — drift sentinels for all four fixes plus a fixture-arithmetic gate (`scripts/floor-invariant-fixture-check.mjs`, node built-ins, no node_modules) asserting `expected-displayed == manifest − opt-outs`.
- **`tests/unit/floor-department-invariant.test.ts`** (vitest, wired into CI) — the authoritative behavioral proof: runs the real reseed + resolver + scoped board query on a throwaway migrated DB and asserts displayed == manifest − opt-outs, App Development + Engineering distinct, foreign-company row excluded, and idempotent across two seed passes.
- **`tests/fixtures/floor-invariant/{manifest.json,expected-displayed.json}`** — the shared golden the gate and the vitest suite both consume.

### chore(qc): reconcile stale qc-cc.sh check 10.17 with the BUILD-04 deploy.sh deprecation
- **`scripts/qc-cc.sh`** — after `BUILD-04` turned `deploy.sh` into a thin shim that FORWARDS to `atomic-deploy.sh`, check 10.17 (which greps `deploy.sh` for `pm2 save`) started failing on main even though the OOM/reboot-survival guarantee is fully preserved via the forward (atomic-deploy.sh's `pm2 save`, gated by 10.16). Check 10.17 now accepts EITHER an inline `pm2 save` OR the `atomic-deploy.sh` forward. Independent, revertable one-liner made to unblock the qc-cc CI gate on green — not part of the floor-invariant change.

## [v4.69.1] — 2026-07-08 — fix(tasks): idempotency key takes precedence over the title-window dedupe (anthology card collision)

Fixes a real collision: one contact enrolled in two different anthologies produced two cards sharing the same title, and Command Center's generic Layer-2 dedupe (title + workspace window) was collapsing them into a single task row instead of the two distinct rows their distinct idempotency keys called for. A caller-supplied idempotency key (Layer 1) is an authoritative, deliberate identity and must never be overridden by the generic same-title safety net meant for keyless callers (operator UI, plain Telegram capture).

### fix(tasks): Layer 1 (idempotency_key) gates Layer 2 (title window)
- **`src/lib/tasks.ts`** — the title+workspace dedupe window now only runs `if (!input.skipWindowDedup && !input.idempotency_key)`. When a request carries a distinct idempotency key that Layer 1 already checked and found no prior task for, the row is knowingly distinct and the title window must not fold it onto a same-titled neighbour. Keyless callers are unaffected — Layer 2 still guards them against accidental same-title duplicates.
- **`tests/unit/task-ingest-dedup.test.ts`** — three new cases: (7) distinct idempotency keys + identical title within the window → 2 tasks (the regression this fixes), (8) same idempotency key still dedupes across the window (Layer 1 unchanged), (9) keyless same-title within the window still dedupes (Layer 2 preserved for keyless callers).

### Verification
- `tsc --noEmit`: clean (rc=0).
- `tests/unit/task-ingest-dedup.test.ts`: 14/14 pass. Independently reproduced the regression by reverting only `src/lib/tasks.ts` to its pre-fix `origin/main` content against this branch's test file: exactly test 12 (Layer-1-precedence) fails, 13/14 — confirming the fix (not the new assertions) is what makes 14/14 pass.
- `scripts/qc-cc.sh`: 111 checks green, 4 environment-only warnings (fresh-clone has no `.env.local`/live DB — expected).
- `tests/unit/no-naked-dispatch.test.sh`: 5/5 pass.
- `npm run test:vitest`: 95/95 pass.
- `FORBIDDEN_STATUSES = new Set(['done'])` in `src/app/api/tasks/[id]/status/route.ts` — byte-for-byte unchanged; file untouched by this branch — the review→done QC-only (≥8.5) guarantee is intact.
- Repo stays fleet-wide (no client names/IDs/secrets in either changed file); no new npm dependencies. All 5 version locations agree at v4.69.1; annotated tag cut before merge.

## [v4.69.0] — 2026-07-07 — fix(reliability): stuck-in-progress sweep for silent agent failures

Closes the silent-task-hang gap found in tonight's incident investigation: a task dispatched to `in_progress` whose agent turn dies mid-work (repeated tool failures, degenerate loop, thinking-only turn end) without emitting `TASK_COMPLETE:` or writing a terminal status rotted invisibly — the success reconcile (execution-watcher) only matches completions, and stale-task-sweep's in_progress threshold is 24h with a silent backlog-bounce remedy and no operator alert. Nothing marked such a task blocked or surfaced the error until now.

### fix(jobs): stuck-in-progress-sweep — new cron */5 safety net
- **`src/lib/jobs/stuck-in-progress-sweep.ts`** (new) — `runStuckInProgressSweep()`: for an `in_progress` task with no progress past `STUCK_IN_PROGRESS_MINUTES` (default 45) and no recent activity event (liveness guard), transitions it to `blocked` via the audited `transition()` (writing the structured `task_events` row a raw UPDATE skips — the exact audit gap that hid the incident), records the failure in the block metadata columns (`block_audience='SYSTEM'`, `blocked_on_human='operator'`), frees the wedged agent (`working` → `standby`), broadcasts `task_updated`, and alerts the operator once (Rescue Rangers webhook → `notifyOwner` fallback). Blocked is recoverable and visible; the point is a dead task stops being invisible. Opt out with `DISABLE_STUCK_IN_PROGRESS_SWEEP=1`.
- **`src/lib/jobs/scheduler.ts`** — registers the new job (`STUCK_IN_PROGRESS_SWEEP_CRON = '*/5 * * * *'`), alongside the existing `stale-task-sweep` and `interview-nudge-sweep` jobs. No existing job's schedule or behavior touched.
- **`tests/unit/stuck-in-progress-sweep.test.ts`** (new) — DB-backed coverage: blocks a silently-stuck task (frees agent + writes audit event), leaves a fresh `in_progress` task alone, liveness-guard skips an old-but-active task, and respects the `DISABLE_STUCK_IN_PROGRESS_SWEEP` opt-out. 4/4 passing.

### Verification
- `tsc --noEmit`: clean (rc=0). `next lint`: clean (pre-existing warnings only, no errors).
- `scripts/qc-cc.sh`: 111 checks green, 4 environment-only warnings (fresh-clone has no `.env.local`/live DB — expected). `scripts/qc-blocked-gate.sh`: 8/8 pass. `scripts/check-qc-independence.sh`: pass. `tests/unit/no-naked-dispatch.test.sh`: 5/5 pass.
- `node --test` full unit suite (isolated from this box's live OpenClaw gateway + operator ZHC company data): 598/606 pass. The 8 pre-existing failures (`duck-artifact-qc-preview.test.ts`, `duck-s3-s4-lifecycle.test.ts`, `prd-3.4-sentinel-ids-loud-warning.test.ts`, `task-status-transition.test.ts`) reproduce identically against `origin/main` with this branch's changes reverted — confirmed pre-existing and unrelated to this change; no file this branch touches is among them.
- `FORBIDDEN_STATUSES = new Set(['done'])` in `src/app/api/tasks/[id]/status/route.ts` — byte-for-byte unchanged (file untouched by this branch); confirmed no collateral damage.

## [v4.68.0] — 2026-07-07 — feat(anthology): Command Center home tiles, participant token page, and board-producer recognition (W3.1/W3.2/W3.3)

Anthology Engine integration surface, landed from the onboarding-repo side (SPEC 11.2/11.3). Three additive, reversible pieces: Command Center home tiles for the Anthology/Podcast/Interview surfaces, a public self-authenticating participant token page for external co-authors, and status-route recognition of the Anthology board-producer's card marker so `mc_board.py` can move its own board cards.

### feat(home): Anthology, Podcast, and Interview quiet-card tiles (W3.2)
- **`src/app/page.tsx`** — three new destinations alongside the existing seven (PRD 3.8 + F52): Anthology (`/workspace/anthology`), Podcast (`/workspace/podcast`), Interview (`/interview`), each a quiet card with its own icon/chip, no change to the existing seven.

### feat(participant): public, self-authenticating token page for external co-authors (W3.3, SPEC 11.3)
- **`src/app/participant/[[...token]]/`** (`page.tsx`, `GateForm.tsx`, `actions.ts`, `layout.tsx`) + **`src/app/participant/_lib/`** (`gate-engine.ts`, `serialize.ts`) — the ONE public, unauthenticated page surface in the deployment. External co-author participants have no Cloudflare Access login and no `MC_API_TOKEN`; the page authenticates each visitor entirely on its own via a single-purpose scoped token/PIN (HMAC over participant_key + gate id + expiry under `ANTHOLOGY_GATE_TOKEN_SECRET`). A foreign/expired/replayed visitor is refused by the page itself, never by the middleware.
- **`src/middleware.ts`** — `/participant` registered as a subtree bypass in BOTH the early-return auth bypass (exactly like `/api/health`) AND `isInterviewGateExempt`, so the interview shell-lock redirect can never trap an external participant behind `/interview` even if the middleware body is re-ordered later.

### feat(api): Anthology board-producer source-marker recognition (W3.1)
- **`src/app/api/tasks/[id]/status/route.ts`** — the signed status-transition route previously scoped card moves to Skill-6 producer cards only (`source=funnel|survey|web-development`), which would 403 every Anthology card move from `mc_board.py`, defeating SPEC 11.2's requirement that the Anthology Engine move its own cards through backlog → in_progress → review. Added `ANTHOLOGY_SOURCE_MARKER` (`Source: anthology`) + `hasAnthologyMarker` + `isSignedBoardProducerCard` (Skill-6 OR anthology); the scope gate now calls `isSignedBoardProducerCard`. An unmarked task is still out of scope (403); no W3.2/W3.3 file touched.
- **`FORBIDDEN_STATUSES = new Set(['done'])` is UNCHANGED** and is still checked BEFORE the card-scope check — verified explicitly at merge time (line order: forbidden-status check → existence check → card-scope check) — preserving the review→done QC-only (≥8.5) guarantee byte-for-byte.

### Verification
- `tsc --noEmit`: clean (rc=0). `next lint`: clean (pre-existing warnings only, no errors). `next build`: clean, `/participant/[[...token]]` route compiles and is listed in the route manifest.
- `scripts/qc-cc.sh`: 111 checks green, 4 environment-only warnings (fresh-clone has no `.env.local`/live DB — expected, matches documented pattern for a bare checkout).
- `scripts/qc-blocked-gate.sh`: 8/8 assertions pass. `scripts/check-qc-independence.sh`: pass. `tests/unit/no-naked-dispatch.test.sh`: 5/5 pass. `npm run test:vitest`: 95/95 pass.
- Config-guard invariants manually verified: `config/departments.json` is `[]`, `config/company-config.json` is at template state (`companyName: 'Your Company'`, empty industry, empty departments) — no client data introduced.
- `FORBIDDEN_STATUSES` invariant read directly from the merged file: `'done'` is the sole entry and its check (line ~264) executes before the producer-scope check (line ~287) — the done-always-403-first security invariant is intact.
- `npm run test:unit` (the full per-file suite) could not be driven to completion in this sandboxed merge environment: 3 of ~90 test files (`task-ingest-dedup.test.ts`, `prd-1.6-async-persona-selection.test.ts`, `prd-3.4-sentinel-ids-loud-warning.test.ts`) hang indefinitely because each spawns its own `tsx`/esbuild transform-service child process whose IPC ping never returns in this shell sandbox — confirmed NOT a regression by running the identical suite against pristine `main` (`ff4d028`) in a separate clone: the exact same 3 files hang identically, with zero code from this branch in the call path of any of them. Repo stays fleet-wide (no client names/IDs/secrets in any changed file); no Anthropic model ids introduced in source; no new npm dependencies. All 5 version locations agree at v4.68.0; annotated tag cut before merge.

## [v4.67.1] — 2026-07-06 — fix(seed): one malformed departments.json entry no longer wipes the CEO board departments

Bug fix for the operator's real "not all departments showed" report on the CEO board / company-health rating. A `config/departments.json` entry that is a bare string, or an object missing both `slug` and `id`, resolved to `undefined` at `findCanonicalWorkspaceId(db, undefined)`, whose `slug.toLowerCase()` threw "Cannot read properties of undefined (reading 'toLowerCase')". Both the first-boot `autoSeedFromDepartmentsJson()` and the converge-time `reseedWorkspacesFromConfig()` call it once per department inside a single loop wrapped in ONE try/catch, so the throw ABORTED the entire seed — every department after the bad entry (or all of them, when a bad entry came first) silently failed to seed. Net effect for a `departments.json` that carries a malformed entry: partial or ZERO departments seeded, so the board showed nothing / an incomplete set.

### Fix (defensive; no behavior change for well-formed configs)
- **`src/lib/db/task-dedup.ts` — `findCanonicalWorkspaceId()` null-guard.** Returns `null` for an unusable slug (empty/non-string) instead of throwing, so a bad entry can never crash the caller.
- **`src/lib/db/migrations.ts` — skip-and-log malformed entries.** Both `autoSeedFromDepartmentsJson()` and `reseedWorkspacesFromConfig()` now skip any entry that is not an object or lacks both `slug` and `id`, logging it and continuing, so every WELL-FORMED department still seeds. A single bad entry no longer takes the whole board down.

### Verification
- Ran the REAL seed (`db.exec(schema)` + `runMigrations` → `autoSeedFromDepartmentsJson`, plus a follow-up `reseedWorkspacesFromConfig`) against a worst-case fixture whose malformed entries come FIRST: `["a-bare-string", {"bad":1}, {marketing}, {sales}, {support}]`. Result: the two malformed entries are skipped with a log line and `marketing`, `sales`, `support` all seed (3 valid). The pre-fix code aborted the loop on the first bad entry (0 seeded). `findCanonicalWorkspaceId(db, undefined)` now returns `null` instead of throwing. The `computeCompanyHealth` grading engine is untouched.
- Repo stays fleet-wide (no client names/IDs/secrets in either changed file); no Anthropic model ids introduced; no new npm dependencies. All 5 version locations agree at v4.67.1; annotated tag cut before merge.

## [v4.67.0] — 2026-07-06 — feat(interview): audio-or-text restored, operator-triggered Telegram start link, durable answers-document export, owner-send gateway fix, phone-safe walkthrough

Interview-experience diagnostic + hardening pass across the six operator questions (start/trigger, mobile + Telegram link, save/pause/resume, audio vs text + custom questions, completion, the durable answers document). Repo stays fleet-wide (no client names/IDs/secrets in any changed file); no new npm dependencies; no Anthropic model ids. `tsc --noEmit` clean, `next lint` clean (pre-existing warnings only), `next build` clean, `lint:interview-jargon` PASS, 18 new unit tests green and the full `test:unit` suite matches the pre-change main baseline exactly (the 12 environment-dependent failures reproduce byte-for-byte on pristine main). All 5 version locations agree at v4.67.0; annotated tag cut before merge.

### fix(notify): owner Telegram sends were ALL silently failing
- **Root cause.** `src/lib/notify.ts` invoked `openclaw message send --to <id> --text <msg>` — but the CLI has no `--to`/`--text` flags (they are `-t/--target` and `-m/--message`; verified against installed openclaw 2026.6.11). commander rejected every call, `execFileSync` threw, and every owner send (interview nudge sweep, QC notices, task-lifecycle pings) returned false. Fixed to `--target`/`--message` with a flag-contract test so the dead flags can never return.
- **Mac boxes could never resolve the owner chat.** The workspace base was hardcoded to `/data/.openclaw/workspace`; now env override → `/data` only when it exists → `~/.openclaw/workspace` (same probe order as the Skill-23 scripts and the interview seam).
- **Fleet-parity owner resolution.** Resolution now mirrors openclaw-onboarding `shared-utils/resolve-owner-chat.sh`: `OPENCLAW_OWNER_CHAT_ID` env pin → `channels.telegram.allowFrom` → `commands.ownerAllowFrom` → paired direct sessions, every source rejecting the full 3-id operator denylist (was 1 id), and the legacy "any direct session" fallback (which could target an operator DM) is removed.

### feat(interview): operator-triggered "when you're ready, start here" link (`POST /api/interview/send-link`)
- One sanctioned, deliberate way to hand the owner their interview link over Telegram — through the OpenClaw gateway only. START mode sends `<dashboard>/interview`; once an interview is underway it sends the P0-7 slug-contract resume link. Bearer `MC_API_TOKEN` (same gate as `/api/system/converge`); 30-minute cooldown via an `interview_link_sent` events row (`force:true` for a deliberate re-send); ledger written only on confirmed delivery; no chat ids or secrets in response or ledger; 409 when the interview is already complete. Operator-triggered ONLY — nothing auto-fires it.

### feat(interview): the durable answers DOCUMENT is now retrievable on demand (`GET /api/interview/answers/export`)
- Byte-faithful export of the canonical `company-discovery/workforce-interview-answers.md` — every question asked + every answer given, with Logged timestamps and provenance, appended incrementally as the owner answers, so the export is complete at any moment (mid-interview, after a pause, after completion). `?download=1` serves an attachment keyed to the company slug; calm 404 when nothing is recorded yet; read-only through the seam (WG-10c intact). ReviewScreen gains "Download a copy of your answers".

### feat(interview): voice input restored — every question answerable by SPEAKING or typing
- New shared `DictateAnswerButton` (Web Speech API via the existing MicDictateButton, interim-transcript merge) wired into BOTH input surfaces: the structured QuestionCard text control and the free-form ConversationPane. Dictation lands in the same controlled field and submits through the same `/api/interview/answer` / `/api/interview/turn` write paths, so every anti-fabrication/provenance gate is inherited. Browsers without SpeechRecognition degrade to a disabled mic + tooltip; iOS Safari/Chrome/Edge keep both modes.

### fix(walkthrough): the interview walkthrough is reachable on phones
- At 375px the walkthrough card hung off-screen with Close/Skip/Next untappable (no Escape key exists on a phone). Two stacked causes fixed: anchored placement used a fixed 420px width (now capped `min(420px, 100vw − 32px)` in both computePosition and the style); centered placement relied on `transform: translate(-50%,-50%)` which framer-motion's scale animation clobbers (now `inset:0 + margin:auto + fit-content` — no transform). Verified live at 375×812: card spans x=16…359, Close tappable.

## [v4.66.0] — 2026-07-06 — feat(ui): bottom-cutoff fix (dvh + min-h-0 scroll chain), true mobile/tablet responsiveness, and a data-first main-screen rebuild

Design + layout overhaul pass driven by three operator complaints: content CUT OFF at the bottom of the page, broken responsiveness on phones/tablets, and a main screen that looked like a marketing splash instead of a command center. Root causes were found, fixed, and verified end-to-end with before/after screenshots at 375/768/1440 plus a 700px-short-viewport probe. Repo stays fleet-wide (no client names/IDs/secrets in any changed file); every new accent uses the themeable `brand-*` utilities or `--brand-*`/`--bcc-*` variables so white-label re-theming (BrandTheme D2) keeps working; no Anthropic model ids introduced; no new npm dependencies. `tsc --noEmit` clean, `next lint` clean (no errors), `next build` clean. All 5 version locations agree at v4.66.0; annotated tag cut before merge.

### The bottom-cutoff bug — root cause + fix
- **Root cause (two stacked layout faults).** (1) The board shells (`/tasks/all`, `/workspace/[slug]`) used `lg:h-screen` + `lg:overflow-hidden`, and `100vh` on mobile Safari/short windows includes the space under the retractable browser chrome — so the shell was measured TALLER than the visible viewport and its bottom row rendered underneath the chrome, unreachable because the page itself cannot scroll. (2) Inside the shell, the flex chain (`MissionQueue` root → board region → column task lists) had NO `min-h-0`: flex items default to `min-height:auto`, so the column lists silently grew past the fixed shell and the overflow was clipped by `overflow-hidden` with no scrollbar and no affordance — cards visually amputated at the fold.
- **Fix.** Shells are now `min-h-dvh lg:h-dvh` (dynamic viewport height tracks the REAL visible area) with `min-h-0` at every level of the scroll chain (`flex-1 min-h-0` board row, `min-w-0 min-h-0` MissionQueue root, `lg:min-h-0` columns + task lists). Column lists gained `lg:pb-6` (the last card never kisses the hard clip edge) and `overscroll-contain`. Root layout body/wrapper moved from `min-h-screen` to `min-h-dvh`.
- **Verified.** Playwright probe at a 700px-short 1440 viewport: the deepest card of an overflowing column scrolls fully into view inside its column (`lastCardFullyVisible: true`, 64px clearance); page-level audit across 7 routes × 3 breakpoints shows zero horizontal overflow and zero unreachable clipped regions.

### Responsiveness (375 / 768 / 1280–1440)
- **Departments sidebar is a real drawer below lg (`src/components/AgentsSidebar.tsx`).** It used to render inline FULL-WIDTH above the board on phones/tablets, pushing the actual Kanban ~700px below the fold. Now: `hidden lg:flex` inline rail on desktop; below lg an overlay drawer (`fixed inset-y-0`, `w-80 max-w-[85vw]`, scrim closes on tap) — both the all-departments and focus-mode rails.
- **The Header finally renders its drawer opener (`src/components/Header.tsx`).** `Header` always accepted `onMenuClick` but never rendered a button for it — on mobile there was literally no way to open the department list. A `lg:hidden` hamburger (44px target, `aria-expanded`) now precedes the logo.
- **Home header no longer collides at 375px (`src/app/page.tsx`).** The old landing header wrapped "Command Center" INTO the live clock ("Command Center13:21:53" overlap in the before screenshot). The rebuilt top bar truncates gracefully, hides the wordmark/clock at narrow widths, and keeps one status chip.
- **CEO-board hero stat pills wrap (`CompanyHeroCard.tsx`).** The Total-Tasks / Active-Agents / Completion-Rate pills were clipped off BOTH screen edges at 375px (no wrap); now `flex-wrap justify-center` + responsive padding.
- **Status contradiction removed (`Header.tsx`, `SystemStatusPill.tsx`).** The header showed a red "OFFLINE" (system probes) directly beside a green "Online" (gateway) — the app looked like it was arguing with itself. The pills now name their subject ("Systems Live/Down…", "Gateway Online/Offline"); the gateway pill hides below sm where the system pill is the single status.
- **Pinch-zoom restored (`src/app/layout.tsx`).** `viewport.maximumScale: 1` blocked zoom on phones (WCAG 1.4.4 failure) — removed.

### Main screen rebuilt as an operations overview (`src/app/page.tsx`)
- **Before:** seven full-bleed rainbow-gradient billboards ("Choose where you want to go") with ZERO live data — the brand color appeared nowhere, every card screamed equally, body copy sat at white/75 on saturated gradients, and a 5xl centered welcome burned ~300px of prime viewport. **After:** a data-first overview applying researched patterns from best-in-class ops dashboards (Linear's restraint/hairline discipline, Stripe/Vercel's KPI-row + F-pattern hierarchy): greeting + date; a 4-up KPI strip (Active / In progress / Completed / Needs attention — live `/api/tasks` numbers, JetBrains Mono tabular, each a link into the board, blocked>0 renders red); a **department pulse rail** (each department as a chip with its real emoji, open-task count, and a live working/idle dot from agent status — links to that department's focus board); all SEVEN destinations preserved as quiet white cards (10%-tint icon chip, one-line description, live count badges) in a grid that fits above the fold at 1440×900; a recent-activity feed (real `/api/events`, internal plumbing events filtered out with a fall-back so the empty state is never falsely shown, machine `[TAG]` prefixes stripped) beside a System card (gateway health, agents working/total, department count). Honest empty/loading states throughout ("No activity yet. Dispatch your first task…"); one 250ms fade-in that respects `prefers-reduced-motion` (replaces 7 staggered spring entrances); the clock ticks per-30s instead of re-rendering every second.
- **All numbers are real or absent — never fabricated.** KPIs derive from the live task list; the pulse rail from live agent/workspace rows; health-rating surfaces (CEO board grades, department health) untouched.
- **CEO board backdrop fixed (`src/app/ceo-board/page.tsx`).** The page painted `url('/holo-bg.png')` — a file that has never shipped (404 on every load). Removed; quiet `bcc-bg` surface + the sticky header got a real `bg-white/90 backdrop-blur` treatment instead of flat gray floating over content.
- **White-label hardening (`CompanyHeroCard.tsx`).** The hero gradient + grade-label color were hardcoded BlackCEO greens (`#1B5E20`/`#2E7D32`/`#81C784`); now driven by `var(--brand-900/800)` + `text-brand-300`. (Uses the CSS vars directly because BrandTheme's generated `from-*/to-*` overrides reset `--tw-gradient-to` in descending-step order, which blanks `from-brand-900 → to-brand-800` gradients.)
- **Design system left clean + themeable for the operator-reference second pass:** no new tokens invented; everything rides the existing `bcc-*`/`brand-*` scales.

### Verification
- BEFORE/AFTER screenshots at 375/768/1440 for home, kanban (all-tasks), CEO board, department health, settings, intelligence, and the department picker saved to the operator's Downloads (`command-center-redesign/`).
- Layout audit (custom Playwright harness): 7 routes × 3 breakpoints — zero horizontal document overflow; no unreachable clipped content; drawer open/close verified on a real 375px viewport; bottom-of-column reachability proven at a 700px-short desktop viewport.
- `npx tsc --noEmit` clean · `npm run lint` zero errors · `NODE_ENV=production next build` clean.

## [v4.65.0] — 2026-07-06 — feat(dispatch): Layer A — departments-that-use-skills (task → skill matcher)

The CEO now hands the doer the installed CAPABILITIES most relevant to a task, not just a specialist + SOP. `matchSkillsForTask()` (in `src/lib/context-pack.ts`) globs the box's installed `SKILL.md` files, dept-scopes the candidates via the onboarding `skill-department-map.json`, scores the task text against each skill, and returns the top-3 — which are rendered into the dispatch message as a **"SKILLS AVAILABLE FOR THIS TASK"** block so the specialist reaches for the right playbook instead of reinventing a capability it already has. Wired into BOTH dispatch paths (auto + manual). Repo is fleet-wide — no client names, chat IDs, operator paths, or secrets in any changed file; no Anthropic model ids in any client-facing path. `tsc --noEmit` clean; full vitest CI suite green (95); new node:test suite green (8); qc-cc.sh PASS (111 green). All 5 version locations agree at v4.65.0; annotated tag cut before PR.

- **Skill matcher reuses the department-router's own machinery (no second embedding stack).** Scoring goes through the SAME `getEmbeddingApiKey()` / `fetchEmbeddings()` / `cosineSimilarity()` used by `department-router.ts`: when the CLIENT'S OWN embedding key is configured, each skill's `name. description` is embedded alongside the task text and ranked by cosine, keeping only matches at/above a `SKILL_MATCH_FLOOR` (default 0.55, env-overridable). With no key — or on any API error, or when nothing clears the floor — it falls back to keyword-overlap scoring (reusing the pack's `keywordsFromTask`), so the feature works with zero configuration. Client sovereignty preserved: the client's own key, never a shared/operator one.
- **Dept-scoped candidates via the onboarding `skill-department-map.json` (resolved on-box, env-overridable).** The map is resolved through the per-platform onboarding locations (or pinned with `SKILL_DEPARTMENT_MAP` / `CC_SKILL_DEPARTMENT_MAP`) and normalized from any of three shapes — `{departments:{dept:[skills]}}`, `{skills:{skill:[depts]}}`, or a bare `{skill:[depts]}` — to `skill → canonical-dept-slug[]` (via `canonicalDeptSlug`). Scoping rules: a skill mapped to another department is excluded for the routed dept; an UNMAPPED skill is globally available; a skill mapped to a global token (`*`/`all`/`global`/…) is available to every department; a task with no department sees all skills.
- **Skill roots are env-overridable and symlink-aware.** `SKILL_SEARCH_ROOTS` / `CC_SKILL_ROOTS` pin the exact roots (test/CI determinism); otherwise the common on-box skill install locations + the onboarding-shipped skills tree are probed per-platform. The `SKILL.md` walk is depth- and count-capped (never hangs dispatch on a misconfigured root) and stat-follows symlinked skill directories — plugin skills under `~/.claude/skills` are frequently symlinks, whose `Dirent` reports neither file nor dir, and would otherwise be silently missed.
- **Never throws on the dispatch hot path.** `matchSkillsForTask()` degrades to `[]` on ANY failure (documented contract); both dispatch call sites additionally wrap the call so a skill-match failure can never block a dispatch. `ContextPack` gains a `matched_skills: MatchedSkill[]` field (defaults to `[]`, never undefined) and a new `'skill'` `DocPointerKind`; `renderContextPackSection()` renders the block via the shared `renderMatchedSkillsSection()` helper so the manual route — which does not build a full pack — renders it identically.
- **Auto path (`src/lib/task-dispatcher.ts` → `autoDispatchTask`)** computes the matches (async, before the synchronous pack builder) and threads them into `buildContextPack({ …, matchedSkills })`. **Manual route (`src/app/api/tasks/[id]/dispatch/route.ts`)** computes the matches and splices `renderMatchedSkillsSection()` into the task message for operator-clicked-dispatch parity.
- **Tests (`tests/unit/departments-use-skills-layer-a.test.ts`, node:test):** globbing + keyword ranking (most-relevant skill first, unrelated skills excluded), top-3 cap + explicit `limit`, dept-scoping (mapped-elsewhere skill excluded / included for its own dept / unmapped skill global), symlinked-skill discovery, empty-roots → `[]`, the "SKILLS AVAILABLE FOR THIS TASK" render, and `buildContextPack` threading `matched_skills` through to the render. No network — every embedding-provider key is cleared so the keyword path is deterministic.
- **Independent QC (clean-code-guard skill):** reviewed for correctness / robustness / clean-code. No high or critical findings; the one low-severity real-world coverage gap (symlinked skill dirs skipped by `Dirent` type checks) was fixed and covered by a test. Independent QC score: 9.0/10 — PASS.

## [v4.64.0] — 2026-07-06 — feat(interview): AI Workforce Interview overhaul — bulletproof pause/resume continuity, known-context memory, 3 contract-bug fixes, question-set upgrade, design-system unification (July-3rd-app hardening)

MASSIVE improvement pass over the `/interview` surface (the AI Workforce Interview app that gates Command-Center provisioning, built in the July-3rd update). Locate→analyze→fix→verify: three CONTRACT BUGS that broke core flows in production were found and fixed, pause/resume continuity is now server-derived and exact, the interview remembers what it already knows (confirm-or-correct with real provenance), the structured question set gained a designed-but-never-wired operations section, and the last off-design screen was folded onto the shared design system. Every canonical-write doctrine is preserved: files stay the single source of truth, all writes still go exclusively through the Skill-23 scripts, no new trigger paths, no Anthropic models, no client names. Fresh live verification end-to-end (structured flow → refresh-resume → skip/circle-back → department board 28/28 → provenanced yes/no decisions → triple-gated complete → locked-shell unlock → mobile 390px no-overflow).

- **BUG FIX 1 (P0) — every structured card submit 400'd.** The cards posted `{questionId, storeOn, kind, value}` while `/api/interview/answer` required `{answer, prompt?, …}` — schema validation failed on EVERY structured answer ("Something went wrong saving that"). All cards now build their body through ONE shared builder (`src/lib/interview/answer-payload.ts`), the route's schema moved to `src/lib/interview/answer-schema.ts` (now `.strict()`), and `tests/unit/interview-answer-contract.test.ts` parses the builder's output with the route's OWN schema for every question — client/route drift now fails CI instead of failing owners.
- **BUG FIX 2 — the "Build my company" button died whenever a chat session existed.** ReviewScreen POSTed `{sessionId}` to `/api/interview/complete`, whose `.strict()` schema rejects unknown keys → 400 on the one button the whole interview leads to. The trigger now sends the empty body the route expects.
- **BUG FIX 3 — logo file-drop never worked.** LogoDropCard produces `data:image/*` URLs (≤2MB), but the route rejected everything non-http(s) AND capped answers at 20k chars. The route now accepts bounded inline images: the FULL data URL mirrors onto `clients.logo_url` (BrandTheme renders it) while the transcript records a short human line ("Uploaded a logo file (image/png, ~240 KB)…") instead of a megabyte of base64.
- **CONTINUITY — resume is exact, server-derived, and survives everything.** New pure module `src/lib/interview/structured-progress.ts`: `/api/interview/state` matches transcript Q/A blocks against the question set (same normalization as the answers route) and reports `structured.{answeredIds, nextIndex, complete}`. The client reconciles from that server truth, so a refresh / new browser / Telegram hop lands on the FIRST unanswered card — never consent, never question 1, never a duplicate transcript block (previously `structIndex` was client-only state: a refresh restarted the deck or dumped the owner into the conversation with cards unfinished). WelcomeBack shows the exact next question in its own words. The gateway conversation session id persists per interview (localStorage keyed by the seam's stable `interviewSessionId`, which `/state` now exposes read-only) so a reload continues the SAME interviewer session. Structured skips persist per device and surface as circle-back chips that re-open the exact card.
- **MEMORY — the interview uses what it already knows.** `/state` exposes `knownContext` (client record + company settings, placeholder values filtered): the matching card prefills the fact and offers "Confirm & continue" — an unchanged confirm posts `confirmedFromContext`, recorded as the `confirmed-from-context: <source>` provenance QC check #5 expects (the API supported this; nothing used it). Later prompts personalize with the company name on screen ("What feeling do you want the Verify Labs brand to evoke?") while the CANONICAL prompt is always what's recorded, so matching/QC stay exact (possessive-aware replacement). The conversation kickoff/resume turns brief the interviewer with the owner's name + a don't-re-ask instruction.
- **DEAD-END FIX — a slow interviewer reply no longer forces a resend.** `/api/interview/turn` gains a read-only GET (`?sessionId&after`) and the client polls it for up to 90s after a `pending` turn, delivering the reply when it lands. A progress-stamp failure after a successful transcript append (`appended:true` on 502/503) is now a SOFT success — the owner advances (the answer IS saved) instead of being stuck on an un-fixable card.
- **QUESTIONS — better set, zero duplication.** Identity + operations questions now live in ONE client-safe module (`src/lib/interview/base-questions.ts`) imported by both the Node question set and the client (they were hand-duplicated, drift-prone). company_name gains help copy (brand vs legal name); industry's help explains WHY it's asked (drives department recommendations; specific beats generic). NEW `command_center_name` question completes the pre-plumbed `operations` section (labels/milestones existed with zero questions): optional, jargon-free ("your company's home base"), fills `company.commandCenterName` — previously only settable by an operator post-closeout. Branding stays vendored/canonical (sync test untouched, PASS).
- **SYSTEM CONNECTION — interview answers now land where the unlocked dashboard reads.** `client.name` answers mirror onto the clients row + `companyName` in company-config; `company.industry` / `company.commandCenterName` mirror into `config/company-config.json` via a new atomic-merge helper (`src/lib/interview/company-mirror.ts`, same allow-list as the Settings route). Best-effort/files-win: a mirror failure never unwinds a canonical write. Progress stamps now actually advance during the structured phase (cards send `questionNumber` + `phase`; previously nothing was stamped, so the rail sat at 0%).
- **UI/UX — one design system, honest states, accessible.** WelcomeBack restyled from its hardcoded slate/indigo palette onto the shared `iv-*` tokens (it now re-themes to the client's brand like every other screen) and gained next-up prompt + structured circle-back chips. ProgressRail: server-derived percent (one formula, one place), live "N answers saved" truth line + a "Saved just now ✓" flash on every save (aria-live), semantic `<ol>` steps with `aria-current`, real `progressbar` semantics. ConversationPane announces interviewer turns (aria-live). `MotionConfig reducedMotion="user"` respects OS reduced-motion across all interview animation. Consent screen: picking "What happens with my answers?" relabels Begin ("I'm ready — begin interview") so learning isn't a dead end, plus a "Checking for saved progress…" boot line. New `.iv-known-note` / `.iv-chip` tokens ride the brand variables.
- **Tests:** +16 unit tests across 2 new suites (contract pinning incl. legacy-shape rejection + oversize/inline-logo bounds; resume math incl. mid-deck pause, non-contiguous answers, skip interaction, personalization canonicality). Full suite: 581 tests, zero new failures (the 8 pre-existing failures on main are outside the interview surface). `tsc --noEmit` clean, `next build` clean, interview-jargon lint PASS (17 files), branding sync-test PASS.
- **Fresh live verification (sandboxed HOME + real Skill-23 scripts v17.0.17):** structured answers append byte-shaped transcript blocks + stamp state (`lastQuestionNumber` advances, `interviewSessionId` minted); refresh mid-deck → WelcomeBack "4 answers saved" → resumes at the exact next card; skip → chip → circle-back re-opens the exact card; conversation degrades gracefully with the gateway down and the transcript gate still unlocks departments; board renders the LIVE 28-dept floor, YES/NO land as fully-provenanced decision objects via `record-dept-decision.sh`; review lists every answer; complete pre-flight + `update-interview-state.sh --complete` → `interviewComplete=true`, QC pending → calm needs-review (never a false pass); locked shell verified both ways (/ → 302 /interview before; dashboard unlocks after); 390px mobile: no horizontal overflow.

## [v4.63.0] — 2026-07-06 — feat(dashboard): full UX/design + kanban + settings + models + health-rating + responsive improvement pass

A cross-cutting improvement pass across the whole Command Center dashboard: a cohesive design/nav system with real mobile navigation, a kanban board whose drag/move/CRUD actually persist (Blocked is finally reachable, errors surface instead of silently snapping back), honest settings + a fixed models engine (the Ollama-Cloud cascade now actually selects; operator model overrides now win), a truthful company/department health-rating (no fabricated 72s / hardcoded B grades), and verified responsive layout at mobile/tablet/desktop. Repo is fleet-wide — no client names, chat IDs, GHL location IDs, operator paths, or secrets in any changed file; box user, not root. No new npm dependencies; no Anthropic model ids introduced into any client-facing path (one hardcoded Anthropic id was REMOVED from the Header model list). All 5 version locations agree at v4.63.0; annotated tag cut.

### Structural / data-layer
- **Fresh-DB seed no longer crashes (`src/lib/db/seed.ts`, `src/lib/db/schema.ts`, `scripts/demo-seed.js`).** A brand-new database failed to seed at all: `agents.workspace_id` defaults to `'default'` with a FK to `workspaces(id)`, but nothing seeded that structural row, so the Orchestrator INSERT threw `SQLITE_CONSTRAINT_FOREIGNKEY`. The seed now guarantees the structural `'default'` workspace first (idempotent, carries no client/demo content). The base `tasks.status` CHECK in `schema.ts` was widened to the 10-status board model (`backlog, inbox, planning, pending_dispatch, assigned, in_progress, review, testing, blocked, done`) so a fresh DB is born in lockstep with `TaskStatus`/`validation.ts` (migration 076 already anticipated this and skips when the widened CHECK is present). `demo-seed.js` used invalid priority literals (`normal`/`urgent`) that fail the priority CHECK — corrected to `medium`/`critical`.

### Kanban board (features that actually work)
- **Drag/move errors now surface + revert (`src/components/MissionQueue.tsx`, new `src/components/kanban/BoardToast.tsx`).** A failed status change previously left the optimistic move in place (desync) or silently snapped back. All non-ok responses now revert the move AND raise a dismissible toast with the server's actual reason; the existing "Triad incomplete" 400 still opens the edit modal so the user can fix the missing description/SOP/persona.
- **Blocked is finally reachable (new `src/components/kanban/BlockTaskModal.tsx`).** Dropping/moving a card to Blocked used to 400 every time (the API requires `blocked_reason` ∈ {decision,approval,credential,payment}, `blocked_on_human` ∈ {owner,operator}, and a non-empty `ask`). Moving to Blocked now opens a modal collecting those three fields and PATCHes them; cancel reverts. Verified end-to-end: an in-progress task moved to Blocked persists `status=blocked, blocked_reason, blocked_on_human, ask`.
- **Touch-friendly moves (new `src/components/kanban/MoveTaskMenu.tsx`).** Native HTML5 drag-and-drop does not work on touch, so every card gets an accessible "Move task" menu (real button + `role=menu`) that runs the same status-change path (including the Blocked modal) — the board is now usable on phones/tablets.
- **Real-time deletes (`src/lib/store.ts`, `src/hooks/useSSE.ts`).** The DELETE route already broadcast `task_deleted`, but the store/SSE had no handler — deleted cards lingered. Added `removeTask(id)` + a `task_deleted` SSE case.
- **DELETE 500 fixed (`src/app/api/tasks/[id]/route.ts`).** Deleting any task that had persona-selection/performance history threw a generic 500: `persona_selection_log` and `persona_performance` carry a plain (no-action) NOT-NULL FK to `tasks(id)`, hard-blocking the delete under `foreign_keys=ON`. The handler now clears those task-scoped analytics rows first. Verified: delete now returns `{success:true}`.
- **Create-as-blocked rejected (`src/app/api/tasks/route.ts`).** A brand-new task could be POSTed with `status:blocked`, silently dropping the (uncarried) blocked fields — a card parked in Blocked with no reason. Create-as-blocked now returns a descriptive 400 pointing at the correct flow; the Blocked column's "+" create button is hidden (you reach Blocked by moving an in-flight task).
- **Per-column create + wider status set + search + empty states.** The column "+" seeds the new task's status from that column; the create modal's status dropdown is widened to the real board statuses with human labels; a clearable board search filters by title/description; the no-op "By Status" filter chip (identical to "By Total Tasks") was removed; empty columns show a muted "No tasks" hint.
- **`/tasks/all` resilience (`src/app/tasks/all/page.tsx`).** Added a 60s task refetch so the cross-department board doesn't go stale when SSE is blocked.
- **UI-created tasks keep their department (`src/lib/tasks.ts`).** `createTaskCore` now backfills `department` from the workspace slug when omitted, so UI-created tasks no longer vanish under the `/tasks/all` department filter.

### Settings (honest + persistent)
- **Intelligence Settings clear + lock surfacing (`src/app/api/settings/intelligence/route.ts`, `src/app/settings/intelligence/page.tsx`).** You can now CLEAR a saved model/persona override (a `null`/`''` value deletes the row, respecting the lock check) via a "Reset to inherited" affordance; a locked-setting 423 now names the lock holder + reason instead of a generic "Failed to save".
- **Provider status is honest (`src/components/settings/IntelligenceProviderList.tsx`, `src/app/api/models/provider-status/route.ts`).** The badge that only means "a key string exists somewhere" now reads "Key present" (not "Configured") with a note that presence ≠ verified.
- **Settings hub de-lied (`src/app/settings/page.tsx`, `src/lib/config.ts`).** The page persisted path/URL fields to localStorage that the server never reads (and a `defaultProjectName` field nothing consumes). The dead field is removed; the path/URL values are shown as a read-only "server uses environment variables" reference; the page is a clean hub linking Company + Intelligence settings.
- **Company Settings reflects + reports truthfully (`src/lib/company-config.ts`, `src/components/CompanySettingsForm.tsx`, `src/app/settings/company/page.tsx`).** Brand colors/logo are now read back so the form shows persisted state, and a failed/absent live-branding apply shows a visible "Saved to config, but live branding not applied: <reason>" warning instead of a silent "Saved!".

### Models engine (wiring that applies)
- **Ollama-Cloud cascade actually selects (`src/lib/model-selector.ts`).** `tierOf()` only classified Tier 1 for `ollama/<model>:cloud`, but the Ollama-Cloud connector emits `ollama-cloud/<model>` — so Ollama Cloud fell to Tier 3 (free-only) and the advertised "Ollama Cloud → OpenRouter OSS → Free" cascade never selected it. `tierOf()` now recognizes `ollama-cloud/` (Tier 1) and `ollama-local/` alongside the legacy shape; the docstring is truthful.
- **Operator model overrides now win (`src/lib/intelligence-resolver.ts`).** The role-level and department-level overrides an operator sets in Intelligence Settings were resolved BELOW the auto-selector, so they almost never applied despite the UI claiming authority. The resolution order now places role override and dept default ABOVE the auto-selector (task pin → SOP pin → role override → dept default → auto-selector → sovereign default), still passing explicit picks through the sovereignty/forbidden-model gate (a forbidden explicit pick is skipped with a logged reason).
- **Header model list is dynamic (`src/components/Header.tsx`).** The hardcoded `MODEL_OPTIONS` (which included an `anthropic/…` id and drifted from reality) is replaced with a list loaded from the intelligence API; the Anthropic id is gone.

### Company / department health-rating (truthful scoring)
- **No fabricated scores (`src/components/ceo-board/redesign/DepartmentPulseStrip.tsx`, `src/lib/routing/resolve-department.ts`, `src/components/CEODashboard.tsx`).** The pulse strip fabricated a `72` for empty/low-activity departments; `resolve-department` hardcoded `grade:'B', gradeScore:75` for every department; the CEO dashboard used a headcount-dominated bogus formula. All three now use the real `computeDepartmentGrade`/`computeCompanyHealth` from `src/lib/grading.ts` (honest null-gating on insufficient samples) and render "—"/"Insufficient data" instead of a color-coded lie when there isn't enough data.
- **Honest counts + trends (`src/components/ceo-board/redesign/CompanyHeroCard.tsx`, `KPIStatCards.tsx`).** Active-agent counts now match the real `agents.status` enum (`working`), and the hardcoded "Increased from last week" trend is replaced with a real week-over-week comparison from `/api/performance` (or no trend label when there's no history); velocity is clearly labeled ("per week, 30d avg").

### Design system, navigation, responsive
- **Real mobile navigation (new `src/components/MobileNav.tsx`, `src/app/layout.tsx`).** The retired AppShell left the app with NO mobile nav. A fixed bottom nav (Home/Tasks/Board/Operator/Settings, `md:hidden`, safe-area padding) is mounted app-wide and hidden on the gated `/interview` + `/onboarding` shells; the body gets bottom padding so content is never covered.
- **App-wide command palette (`src/components/CommandPalette.tsx`).** Cmd+K previously listed only the 11 operator sub-routes; it now has a "Navigate" group with every top-level route (including the previously orphaned `/personas` and `/sops`).
- **CEO board header fixed + responsive (`src/app/ceo-board/page.tsx`).** The "Agents" tab pointed at a non-existent `/agent-roster` (404) — repointed to the on-page roster; the header's hardcoded hex colors are tokenized and it no longer overflows on small screens (tabs collapse to a scrollable pill row; non-essential right-cluster items hide below `sm`).
- **Accessible + AA contrast (`src/app/globals.css`, `tailwind.config.ts`, `src/components/Header.tsx`).** `--bcc-text-muted` retoned from `#9CA3AF` (fails WCAG AA on white) to `#6B7280`; icon-only buttons gained aria-labels and focus rings; page backgrounds use the `bcc-bg` token instead of scattered `bg-[#F8F9FB]` literals.
- **Responsive verified at three breakpoints** (Playwright, live): mobile 375px, tablet 768px, desktop 1440px — no horizontal page overflow on Home, Tasks board, CEO board, Intelligence Settings, or Settings; the board scrolls inside its own region; the mobile nav appears at 375, is hidden at ≥768 (top nav takes over).

### Verification
- `npx tsc --noEmit`: 0 errors (clean baseline preserved). `scripts/qc-cc.sh`: PASS (111 green, 4 environmental warnings). `npm run test:unit`: green. Live-verified against a running dev instance: Blocked-move persistence, DELETE now 200, create-as-blocked 400, "Key present" label, honest health "—" states, mobile nav + no-overflow at all three breakpoints.
## [v4.62.5] — 2026-07-05 — chore(release): Rescue Rangers Wave-1 consolidation tag — 3 dependent fixes (Skills-Analysis-Master-Plan)

Consolidated ANNOTATED release tag for the **Skills-Analysis Master Fix-Plan — Wave 1** rescue-rangers train (`fleet-heartbeat/`). The three dependent fixes below were independently QC'd (LLM review ≥ 8.5), freshly verified, and ALREADY MERGED to `main` via PR #140 (train T-w1-rescue-rangers) as skill-scoped `rescue-rangers v1.1.0` WITHOUT a repo-wide tag (per-train tag deferred to this consolidation). This bumps the repo to v4.62.5 so the fix-ledger, CHANGELOG, and git tags agree end-to-end; the fleet rollout stays HELD (repo-only). All new code is pure/deterministic and lives OUTSIDE the running dashboard under `fleet-heartbeat/`, so nothing here touches the Command Center app runtime. Fleet-wide repo hygiene: no client names, chat IDs, GHL location IDs, operator paths, or secrets in any changed file; the durable ticket / answered-claim stores are git-ignored. NO new behavior beyond PR #140 — this is a version/tag/CHANGELOG consolidation only.

- **FIX-RESCUE-06 — Path-A outages POST tickets, deterministic id, auto-resolve fixed (`lib/rescue-outage-ticket.sh`).** `heartbeat.sh` / `remediate.sh` now emit one POST per DOWN row: every row → `{action:escalate}` with a deterministic `ticketId` `<client>-<class>-<YYYY-MM-DD>`; a FIXED row → `{action:answer, statusPrefix:"fixed:"}`; an UNFIXED row stays OPEN under SLA. JSON is built by node via env (nothing in the process list); the auth header is read from the secret store and NEVER printed. 22 shell assertions, `bash -n` clean.
- **FIX-RESCUE-08 — semantic dedup (client + failure-class window; no cap increment).** A repeated outage for the same client + failure-class inside the dedup window folds onto the existing ticket instead of minting a duplicate or incrementing a retry cap.
- **FIX-RESCUE-11 — P3 hardening bundle.** Persist answered-ticket claims (durable across restarts), constant-time comparison for the relay auth token, auth-node rename, and poller error-vs-empty disambiguation.

Repo version rolled to v4.62.5 (5 locations in lockstep via `scripts/bump-version.sh`; `--check` PASS). Annotated tag `v4.62.5` created and pushed BEFORE the release PR (tag-before-merge guard). All three Wave-1 rescue fix IDs stamped in the campaign fix-ledger.

## [v4.62.4] — 2026-07-05 — fix(rescue): Rescue Rangers Wave-0 hardening train — 6 fixes (Skills-Analysis-Master-Plan)

Wave-0 hardening of the Rescue Rangers fleet-recovery path (`fleet-heartbeat/`), landing six independent fixes from the Skills-Analysis Master Fix-Plan. All new code is pure/deterministic and lives OUTSIDE the running dashboard, so nothing here touches the Command Center app runtime. Fleet-wide repo hygiene: no client names, chat IDs, GHL location IDs, operator paths, or secrets in any changed file — client/box/person identifiers are always runtime inputs; the durable ticket DB and the return-verify ledger are git-ignored. Tests: 50 `node:test` cases (tier-router 10, ticket-store 12, return-verifier 25, integration 3) + 18 shell assertions (incidents index) all green; every added shell script passes `bash -n`.

- **FIX-RESCUE-02 — watchdog alarm restore + crash-loop detector (`rescue-receiver-watchdog.sh`).** The receiver watchdog's alarm path was dead (`BOT_TOKEN` hardcoded to `""`) — a down receiver was kickstarted forever in silence. The token is re-armed from the secret store (never printed); a first-detection alarm fires once per down-episode, and a bounded auto-restart budget (`MAX_RESTARTS=5`) now distinguishes a transient stall from a genuine CRASH-LOOP: once the budget is exhausted while still down, kickstarting STOPS and a distinct crash-loop escalation pages the operator to check `rescue-receiver-run.sh` / `.env`. Both alarms are flag-file deduped and re-arm only on a 200 (down→up). Hard `ulimit` CPU/mem caps, no loops, all curl calls `--max-time` bounded. The alarm destination (`FIXER_GROUP_CHAT_ID` / `FIXER_THREAD_ID`) and bot token are loaded from the secret store — never hardcoded — and the path degrades gracefully when unset.
- **FIX-RESCUE-05 — real MEDIUM tier + per-tier walls + queue-cap-above-agent-wall (`lib/rescue-tier-router.mjs`).** Routing defaulted the MOST COMMON tickets (coach-client-agent / how-to) to `tier:"hard"` → `kimi-k2.6:cloud@high`, blowing both the ~570s agent wall and the 600s queue cap and paging a human (live evidence: a ticket abandoned at 602.2s). New MEDIUM tier routes coach/how-to → `deepseek-v4-flash:cloud@low` with a ~210s wall; per-tier agent walls for the light-model tiers; and `deriveQueueTimeout` derives the per-job queue cap to sit STRICTLY ABOVE the tier's agent wall so the agent's own graceful timeout fires first (the brutal queue hard-cap no longer pre-empts it). Extracted as a standalone pure module so it is unit-testable WITHOUT importing the receiver (which carries the client return-leg allowlist). All tiers Ollama-Cloud only (binding 2026-07-01).
- **FIX-RESCUE-07 — durable ticket store, RR- numbers, state machine, SLA auto-escalation (`lib/rescue-ticket-store.mjs`, `rescue-sla-sweep.mjs`, `rescue-report.mjs`, `lib/rescue-receiver-store-hook.mjs`).** The live store was n8n workflow static data — WIPED on every workflow re-import, never GC'd, no durable ids/ownership/SLA/audit. Replaced with a single `node:sqlite` (DatabaseSync, Node ≥ 22.5) WAL database on the operator filesystem OUTSIDE n8n: full schema, monotonic human-facing `RR-000123` numbers, a validated state machine (OPEN→ACK→IN_PROGRESS→{RESOLVED|ESCALATED|NEEDS_HUMAN}→CLOSED + REOPENED) with a `ticket_events` audit trail, ownership, severity-derived SLAs with an auto-escalation sweep, a read view, and a GC pass. FAILS CLOSED (throws a clear error if `node:sqlite` is unavailable rather than dropping tickets); illegal transitions throw; all SQL is parameterized. DB path git-ignored.
- **FIX-RESCUE-09 — verify + gate the VPS return leg behind a per-box smoke test (`lib/rescue-return-verifier.mjs`).** The VPS return-leg (docker-exec back into a client box) delivered blind. A per-box loopback smoke test now records a `verified:true` ledger entry; `isReturnDeliveryAllowed` GATES the SSH/docker-exec return path on it, and an unverified box falls back to Telegram-group-only delivery — NEVER a blind SSH. Scoped by default to `type:"vps"` docker-exec blast radius (`RESCUE_RETURN_REQUIRE_ALL_VERIFIED` opts every box in). The verify ledger is git-ignored (keys are client box aliases).
- **FIX-RESCUE-10 — change-log monthly rotation + `incidents.tsv` index for pattern_flag (`lib/rescue-incidents-index.sh`).** The Path-B change-log was re-parsed as prose on every hot-path `pattern_flag` lookup (O(n) over an ever-growing file). A compact append-only `incidents.tsv` index makes the lookup a single tiny scan; the change-log rotates monthly (current + previous retained, older archived to `change-log-YYYY-MM.md`), and the index self-heals (rebuilds from the log if empty) and prunes to the retention window. 18 self-test assertions incl. legacy-scanner parity on 36 probes.
- **FIX-RESCUE-12 — archive `.bak`/bridge/remote-rescue out of the deploy path + de-furnace the heartbeat cron (`rescue-12-cleanup.sh`, `fleet-heartbeat-cron.sh`, `ai.openclaw.fleet-heartbeat.plist`, `fleet-heartbeat.crontab`, `.gitignore`).** (i) An idempotent, dry-run-by-default cleanup relocates ~60 dated `*.bak*` files, the retired `rescue-rangers-bridge.py` (a historical secret carrier), and the vestigial `remote-rescue/` repo under a git-ignored `_archive/` (moved with a manifest, never deleted) so the live path is legible and the secret-leak surface shrinks; `.gitignore` hardened so none of it can ever be committed. (ii) The `fleet-heartbeat` OpenClaw cron fired a full LLM agent turn (`sessionTarget:isolated`, a `:cloud` model, `toolsAllow:[exec]`) every fire just to shell the deterministic `heartbeat.sh` — an Ollama-Cloud furnace. Replaced with a direct-command cron (`fleet-heartbeat-cron.sh`) run via launchd plist or system crontab exactly like the poller/watchdog: no agent turn, no model, no tokens; failure tails go to the Rescue Rangers room THROUGH the gateway (`openclaw message send`, never a raw curl), chat id from the `.env` (never hardcoded).
- **Final-QC hardening (this train).** Client-name/operator-path clearance before merge: replaced hardcoded operator homes with the repo's `${CLAWD_HOME:-${HOME}/clawd}` / `${HOME}` convention across the new scripts, templatized the launchd plist with `__HOME__`/`__CLAWD_HOME__` install-time tokens, and moved the watchdog's Fixer-topic destination into the secret store. STRUCTURAL no-client-names scan PASS; box user, not root.

## [v4.62.3] — 2026-07-05 — feat(persona): DEP-5/F3.7+F3.9 — multi-persona decomposition + SOP slots (Command Center side) (Persona-Matching-Overhaul)

Command Center half of findings **F3.7** (wire decomposition end-to-end) and **F3.9** (per-step SOP persona slots). A task that spans multiple crafts is now executed as a DISTINCT best-fit persona per sub-task, and an SOP can declare per-step `persona_slot`s that drive the split. Builds on FDN-3/F4.1 dispatch injection (v4.61.0), FDN-2 no-naked resilience (v4.62.0) and DEP-2/F3.4 SOP-aware matching (v4.62.2); the matcher side (DEP-4, openclaw-onboarding) rolls separately, so the CC path is robust to DEP-4 rollout timing (transparent `--slots` retry, single-persona fallback). Additive + fail-closed; migration 088 is idempotent. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **Migration 088 (`src/lib/db/migrations.ts`).** `task_subtask_persona` = `_SUBTASK_PERSONA_DDL` + `slot` column (F3.9) + `idx` on `task_id`. Additive/idempotent — `CREATE IF NOT EXISTS` for a fresh DB, `ALTER`-adds `slot` onto an older hand-run-CLI-created (slot-less) table.
- **Decision + plan (`src/lib/tasks.ts`).** `decideMultiPersona` picks `--combined` when the resolved SOP declares >1 `persona_slot` OR a FREE heuristic probe (`heuristicDecompose`, a faithful pure-regex TS mirror of `decompose-task.py`) yields >1 sub-task on a NON-mechanical task; else single-persona. `resolvePersonaPlanAndPin` persists plan rows, pins the seq-1 primary onto `tasks.persona_*` for back-compat, and enforces the FDN-1 fallback guarantee on every REQUIRED slot (empty required slot → dept-default persona + `persona_fallback` audit event). Never leaves a task naked — falls back to single selection on failure. The single-persona path stays SOP-aware (DEP-2/F3.4 `sopContext` preserved through the merge).
- **`persona-selector.ts` / dispatch.** `selectPersonaPlanForTask` spawns `decompose-task.py --combined`; `loadSubtaskPersonas` reads the plan (tolerant of pre-088 boxes); `broadcastPersonaPlan` re-emits `task_updated`. Dispatch renders a PERSONA PLAN block — one Section-4 load pointer per non-mechanical sub-task by REUSING the FDN-3 `buildPersonaBlock` ×N — `''` for a single-persona task (no regression). Coexists with FDN-2 `ensurePersonaForDispatch` + DEP-2 `rescorePersonaWithSOP`.
- **SOP slots + kanban card.** `PersonaSlot` step-level contract in `src/lib/sops.ts` (`validatePersonaSlot` / `getPersonaSlots`, durable across SOP-edit routes); `PersonaSlotChips` renders one slot chip per sub-task on a decomposed task.
- **Tests (14, all pass):** `dep5-persona-plan-multi` + `dep5-no-naked-persona-plan`. `tsc --noEmit` clean; FDN-3 dispatch 8/8 green; zero new failures vs baseline.
- **Re-land note (v4.62.3).** Merged `origin/main` (through v4.62.2) into `fix/DEP-5` (merge, not rebase → no force-push). Resolved `tasks.ts`: unioned imports; DEP-5's multi-persona decomposition block and main's DEP-2 rescore + FDN-2 dispatch-gate functions all coexist; the create-time single-persona call keeps F3.4's `sopContext`. All 5 CC version locations agree at v4.62.3; annotated tag cut.

## [v4.62.2] — 2026-07-05 — fix(persona): DEP-2/F3.4-cc — SOP-aware persona matching (Command Center side) (Persona-Matching-Overhaul)

Command Center half of finding **F3.4** — the running SOP now informs the persona match. Builds on the merged FDN-3/F4.1 dispatch injection (v4.61.0) and CONSUMES DEP-1's `--sop-slug` / `--sop-name` / `--sop-hints` selector inputs (matcher side, separate train). Additive at the module level plus a bounded, fail-closed behavioral change to the auto-dispatch builder; no schema migration (`sops.persona_hints` already exists — it had five writers and zero readers until now). Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **Creation-time SOP-aware match (`src/lib/tasks.ts`).** `createTaskCore` already runs the SOP auto-suggest BEFORE persona selection; now the winning SOP's `slug` + `name` + parsed `persona_hints` are captured (`sopSelectorContextFromRow` / `loadSopSelectorContextById` / `parsePersonaHints`) and threaded through `resolvePersonaAndPin` → `selectPersonaForTask` to the selector. `persona_hints` parsing is defensive: non-array/malformed JSON → `[]`, and empty/sentinel ids are dropped so a stale hint list can never poison the candidate pool.
- **Dispatch-time RESCORE on SOP change (`src/lib/task-dispatcher.ts`).** Persona selection runs at creation — before we know which SOP will govern the work. When the SOP resolved at dispatch (canonical copy, the `getBestSOPForTask` re-pull, or an operator edit) differs from the one the creation-time selection saw (`task.sop_id` at dispatch entry — most commonly: selection saw NONE and an SOP was resolved here), dispatch re-runs selection WITH the SOP context (`rescorePersonaWithSOP`) and patches the in-memory row so `buildPersonaBlock` DELIVERS the rescored persona in the SAME message. Bounded (single-shot, `PERSONA_RESCORE_TIMEOUT_MS=10s`, no retry loop), and it persists a queryable `persona_rescored_at_dispatch` audit event.
- **NO-NAKED / fail-closed (Skill-23 SOP-07 posture).** The rescore NEVER downgrades a persona the task already carries: a null / mechanical (`no_persona_required`) / sentinel selector result keeps the existing pin untouched — only a concrete, non-sentinel persona replaces it. The whole rescore is non-fatal (dispatch proceeds regardless).
- **Defensive selector glue (`src/lib/persona-selector.ts`).** `selectPersonaForTask` gains optional `sopContext` + bounded-`timeoutMs` params and forwards `--sop-*` flags only when the context carries something usable (`buildSelectorArgv` / `hasSopContext`, exported + unit-tested). On a box whose `persona-selector-v2.py` predates DEP-1, the strict argparse rejects the unknown flags (exit 2 / "unrecognized arguments"); we detect that and retry ONCE without the SOP flags — degrading to a non-SOP-aware match rather than failing the whole task to the department-default fallback. All other failures are NOT swallowed.
- **Contract tests (`tests/unit/dep2-sop-aware-matching.test.ts`, 10 cases):** `--sop-*` forwarding (incl. the multi-persona-hint pool comma-joined), partial-context trimming, defensive `persona_hints` parsing, `sopSelectorContextFromRow` / `loadSopSelectorContextById` from a seeded SOP row, the no-naked never-downgrade invariant (null + mechanical results), and the happy path re-pin + `persona_rescored_at_dispatch` event. `tsc --noEmit` clean; FDN-3 / Point-10 / PRD-1.5 / PRD-1.6 persona suites still green.
- **Re-land note (v4.62.2).** Rebased onto `origin/main` after FDN-2 (v4.62.0) and DEP-11 (v4.62.1) landed. Merge-resolved the `persona-selector.ts` selector-spawn block (kept the DEP-2 `runSelector`/SOP-retry refactor while preserving FDN-2's `PERSONA_SELECT_TIMEOUT_MS`=60s budget) and `tasks.ts` (both the DEP-2 `rescorePersonaWithSOP` and FDN-2 `ensurePersonaForDispatch` functions coexist; imports unioned).

## [v4.62.1] — 2026-07-05 — feat(health): DEP-11/F2.3 — dual-store embedding health on /api/health + fleet heartbeat (Persona-Matching-Overhaul)

Additive observability from the Persona-Matching-Overhaul. Surfaces the TWO parallel embedding stores — the persona index (Gemini-only, `gemini-embedding-2` @3072) and the SOP/routing index (provider-flexible: google @3072 or OpenAI `text-embedding-3-small` @1536) — SIDE BY SIDE on the Command Center health endpoint and in the fleet heartbeat, so an ASYMMETRIC degradation (e.g. an OpenAI-only box that gets semantic SOP routing but keyword-only persona Layer-5) is visible instead of silent. Fully additive: no schema migration, no behavior change to matching or dispatch; the new block never flips the top-level health `status` (keyword fallback still serves — asymmetry is operational, not a downed box). Reads ONLY row metadata (provider/model/dim/counts) — it NEVER reads, resolves, or prints an API key or any secret. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **`shared-utils/embedding_health.py` — the dual-store probe.** A standalone, dependency-free (stdlib `sqlite3`) reader that inspects BOTH stores read-only (`file:…?mode=ro`, 5s busy-timeout) and reports each store's `(provider, model, model_histogram, stale_rows, foreign_provider_rows, semantic_ready, degraded)` plus a top-level `asymmetric` flag + human `asymmetric_detail`. Fail-open / degrade-loudly: it NEVER raises to the caller — a missing DB, missing table, or read error becomes `available:false` + `degraded:true` with a note, not a crash; exit stays 0 unless `--strict` (exit 2 when any store is degraded, for CI/heartbeat gating). Stale = rows on a retired gemini slug (`gemini-embedding-001` retires 2026-07-14); asymmetry fires when one store is semantic-ready and the other keyword-only, OR both are semantic on DIFFERENT providers (split vector space).
- **`src/lib/sop-embeddings.ts` — `getSOPEmbeddingHealth()`.** The TypeScript half / last-resort fallback: reports the SOP store's provider, active model/dims, model histogram, stale count (reused from `countStaleGoogleEmbeddings()` — single source of truth) and semantic-readiness. Never throws; a missing `sop_embeddings` table or DB error returns `available:false` + `degraded:true`.
- **`src/app/api/health/route.ts` — `embeddings` block.** `GET /api/health` gains an additive `embeddings` object computed by spawning `embedding_health.py` (`execFile`, 4s timeout, one retry, `python3 -s`) and passing only the resolved SOP provider NAME (never a key). If python3/the script is unavailable it falls back to the TypeScript SOP snapshot (`source:'ts-fallback'`, persona index reported as indeterminate). The block is additive and its degradation never changes the top-level `status`; the route still returns 200 on error.
- **`scripts/cc-health-check.sh` — heartbeat wiring (NON-GATING WARN).** The fleet heartbeat now runs the probe and logs a one-line side-by-side summary (`persona_index=… | sop_index=…`), emitting `WARN: embedding stores ASYMMETRIC …` when the split is present. It NEVER changes the green/red verdict or exit code (operational, not a correctness fault) and attaches the full JSON to the heartbeat payload under `embedding_health`.
- **Tests.** `shared-utils/test_embedding_health.py` (7 cases — both-healthy/not-asymmetric, OpenAI-only headline asymmetry, split-provider asymmetry, retired-model stale+degraded, missing-DB no-crash, provider-none keyword-only, line-format) and `tests/unit/embedding-health.test.ts` (5 cases — empty/healthy/stale/no-key/provider-mismatch) both green; `tsc --noEmit` clean on the changed TS.

## [v4.62.0] — 2026-07-05 — feat(persona): no-naked-tasks invariant — CC resilience half (F3.1-cc) — Persona-Matching-Overhaul FDN-2

Foundation train FDN-2 of the Persona-Matching-Overhaul. Closes the command-center resilience half of the "naked task" gap: every task now provably carries a persona by dispatch time. Lands ON TOP of FDN-3/F4.1 (v4.61.0), which delivers the matched persona to the doer via the shared `buildPersonaBlock` renderer — FDN-2 guarantees that renderer always has a REAL persona to render instead of the unresolved-governance fallback. Additive + non-breaking; no migration. References `DEFAULT_PERSONA_FALLBACK='blackceo-house-voice'`, registered by FDN-1. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **Selector budget 30s → 60s + pinned fallback constants (`src/lib/persona-selector.ts`).** LLM-mode scoring of ~12 finalists × 4 layers plus a cold embedding call can exceed 30s on a loaded box, turning a slow-but-valid selection into a null result (naked task, path A3). The spawn timeout is raised to `PERSONA_SELECT_TIMEOUT_MS = 60_000` with one retry after 5s. Two pinned constants mirror the Python side: `DEFAULT_PERSONA_FALLBACK = 'blackceo-house-voice'` (the generic, brand-neutral fleet persona — deliberately generic so it never out-scores a specialist, registered by FDN-1) and `GOVERNANCE_PERSONA_FALLBACK = 'covey-7-habits'` (the oversight pointer for mechanical tasks). The selector result now carries `governance_persona_id`.
- **1-retry + TS fallback chain (`src/lib/tasks.ts`).** `PERSONA_PIN_MAX_ATTEMPTS` is now 2 (one real attempt + one retry) before the deterministic fallback engages. `deriveDepartmentDefaultPersona` walks the resolved Q2 chain — **last department `persona_assignment` → `company-config.json` `default_persona_id` → `DEFAULT_PERSONA_FALLBACK` constant** — replacing the old synthetic `dept-default-<slug>` id (whose blueprint did not exist) with a REAL, loadable persona; the selector never returns null. A mechanical `no_persona_required` task stays personaless BY DESIGN but records a `[PERSONA-GOVERNANCE]` oversight pointer (`covey-7-habits` else the per-client override).
- **Synchronous dispatch last-chance gate — heal, not stall (`src/lib/tasks.ts` `ensurePersonaForDispatch`, `src/lib/task-dispatcher.ts`, `src/app/api/tasks/[id]/dispatch/route.ts`).** Both dispatch paths now guarantee a persona at send time: if a task reaches dispatch naked (`settings.persona === 'auto'`), the gate applies the deterministic fallback chain and PINS it to the task row (bounded, no Python on the hot path) — it NEVER holds the task for a persona (availability > purity; the fallback makes NULL impossible). The pinned persona then feeds FDN-3's shared `buildPersonaBlock`, so the doer receives a concrete `**Persona (assigned):**` load contract instead of the unresolved-governance fallback.
- **Persona backfill heal-sweep (`src/lib/jobs/persona-backfill-sweep.ts`, `src/lib/jobs/scheduler.ts`).** Modeled on `backlog-redispatch-sweep.ts`: every 5 minutes, re-runs selection for any aged, non-terminal, persona-less task, retroactively healing A3/A4 and self-healing a box recovering from an empty persona universe (A1). Furnace/loop-proof: exactly one attempt per task ever (guarded by a `persona_backfill_attempt` event), a 120s grace window, and a batch cap. Disable with `PERSONA_BACKFILL_SWEEP_ENABLED=0`.
- **Per-client overrides (`config/company-config.json`).** New nullable `default_persona_id` / `governance_persona_id` fields let an operator pin a client-chosen default/governance persona without a code change; both default to `null` (the pinned constants apply).
- **Contract test (`tests/unit/no-naked-dispatch.test.sh`).** An enforced-invariant test (not hope) drives the real CC code across the A1-A7 persona-less paths via the `PERSONA_FIXTURE_JSON` escape hatch against a throwaway fixture DB, asserting: fallback pin on selector failure, governance pointer on mechanical, dispatch-gate heal, F4.1 pinned-persona delivery, and a global "0 non-mechanical naked tasks" invariant. Wired into qc-cc CI. Companion `point10-persona-exhaustion-fallback.test.ts` updated to the new fallback chain.

## [v4.61.0] — 2026-07-05 — fix(persona): FDN-3/F4.1 — deliver the matched persona to the doer at dispatch (Persona-Matching-Overhaul foundation)

Consolidated release of the Command Center foundation work from the Persona-Matching-Overhaul. Bundles the single merged foundation train on this repo — **FDN-3 / finding F4.1**, the highest-value fix in the persona system — into one repo-version roll (v4.60.1 → v4.61.0). Additive at the module level plus a behavioral change to the two dispatch message builders; no schema migration. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root. Independently verified on `origin/main`: both dispatch paths now read `task.persona_id` and deliver the matched persona to the doer.

- **FDN-3 / F4.1 — the matched persona now reaches the executing agent (`src/lib/persona-dispatch.ts`, `src/lib/task-dispatcher.ts`, `src/app/api/tasks/[id]/dispatch/route.ts`).** Both dispatch paths — the fast-loop auto-dispatch (`task-dispatcher.ts`) and the operator-click route (`api/tasks/[id]/dispatch/route.ts`) — previously rendered `settings.persona` and, when it was the string `'auto'`, prose telling the doer to "AUTO-SELECT. Run the 5-Layer Persona Matching Protocol." The persona the selector matched onto the task row (`tasks.persona_id` / `.persona_name` / `.persona_mode`) never reached the doer, so every upstream computation (pipeline, embeddings, 5-layer matching, bonuses, variety, adherence) was discarded at the last hop. A new shared renderer `buildPersonaBlock` (in `persona-dispatch.ts`) is imported by BOTH builders so the two messages are byte-identical for the persona section; it reads `task.persona_id` and emits the same Section-4 (A–D) + §7B load contract that `persona-matching-protocol.md` Step 5 defines — with the id resolved, never self-selected. The AUTO-SELECT / 5-Layer self-selection prose is deleted from both files.
- **`agent_settings.persona` is now an operator LOCK only.** A non-`'auto'` pin via `role_override` / `department_default` hard-overrides the match; `'auto'` now means "defer to the task's matched persona," never "self-select." Task and operator always beat the constant, so client sovereignty is preserved.
- **Never-naked, fail-closed dispatch (Skill-23 SOP-07 posture).** Every branch of `buildPersonaBlock` returns a concrete persona OR an explicit governance oversight pointer — never `'auto'`, never a self-selection protocol, never an empty block. Hybrid tasks render the secondary persona blend; mechanical tasks (`no_persona_required`) carry a governance pointer (`task.governance_persona_id` → `GOVERNANCE_PERSONA_FALLBACK='covey-7-habits'`); unresolved selection falls back to the house governance persona rather than telling the doer to invent one.
- **Contract test (`tests/unit/fdn3-persona-dispatch-injection.test.ts`, 8 cases):** persona-in-message == persona-on-row, Section-4/§7B/[+APPENDIX] present, zero AUTO-SELECT, hybrid secondary, mechanical governance pointer, operator lock, never-naked fallback. `tsc --noEmit` clean. (Landed with the fix in PR #133 @ 1155344.)

## [v4.60.1] — 2026-07-04 — test(interview): Playwright interview-mode lock e2e (WG-6/WG-10c) — proves the /interview shell-lock end-to-end in CI

Follow-up to v4.60.0. Additive test + CI only — no application logic, routes, or schemas changed. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **Playwright interview-mode lock e2e (WG-6/WG-10c) — proves the lock holds (302→/interview while incomplete), exempt paths reachable, and unlock at completion, against the real Edge middleware + HMAC cookie; wired into qc-cc CI.** The suite (`tests/integration/interview-lock.spec.ts`, `playwright.interview-lock.config.ts`, `tests/integration/interview-lock.fixture.ts`, `tests/integration/interview-lock.global-setup.ts`) stands up its own `next dev` server against a throwaway fixture workspace under `test-results/` (never `~/.openclaw`, never the operator's canonical files) and drives the actual `src/middleware.ts` Edge lock plus the sanctioned Node cookie-setter (`refreshInterviewGate` → signed `mc_interview_complete` cookie). It only ever OBSERVES the app-minted cookie — it never forges a token, so no gate is weakened. New `interview-lock-e2e` job in `.github/workflows/qc-cc.yml` runs it headless (Chromium) with `retries: 2` for flake control; `npm run test:e2e:interview-lock`.

## [v4.60.0] — 2026-07-04 — feat(interview): AI Workforce Interview app — the locked /interview surface, seam over the Skill-23 brain, 7-phase flow + triple-gated Build — Big July 3rd Update Wave 5

Wave 5 of The Big July 3rd Update. A new client-facing `/interview` surface that runs the AI Workforce interview (Skill 23 / AI Workforce Blueprint) inside the Command Center, then gates the whole dashboard behind it until closeout. Built by subagents, production-build verified (`npm ci` + `tsc` + `npm run build` all exit 0) and independently verified against a 10-gate integration pass. Additive: the canonical FILES stay the single source of truth — the seam reads them, it never becomes a second write authority. Repo is fleet-wide (no client names, chat IDs, or GHL location IDs in any changed file); box user, not root.

- **Locked `/interview` surface + Edge-safe interview-mode shell lock (`src/middleware.ts`, `src/app/interview/*`, `src/components/interview/InterviewGateSync.tsx`, `src/components/interview/gate-actions.ts`, `src/lib/interview/gate-cookie.ts`).** While the interview is incomplete the middleware 302s every other page route to `/interview`, so the client never lands on an empty dashboard — the full Command Center is the closeout reveal. Edge can't read the DB/filesystem, so a Node server action (`refreshInterviewGate`) derives completion from the two authoritative build-state flags (`interviewComplete` / `buildCompletedAt`) and mints a short-TTL **signed** `mc_interview_complete` cookie that the Edge middleware only READS + verifies. Every non-terminal signal (valid-incomplete, expired-incomplete, absent, forged) **fails CLOSED** to `/interview`; a signature-valid "complete" token is terminal (never reverts). API, `/onboarding`, `/_next`, and asset routes are exempt (no redirect loop).
- **The seam over the Skill-23 brain (`src/lib/interview/seam.ts`, `paths.ts`, `store.ts`, `mirror.ts`).** A pure-fs library that reads the three canonical artifacts — `.workforce-build-state.json`, `workforce-interview-answers.md`, `interview-handoff.md` — and exposes typed readers (`readBuildState` / `readHandoff` / `readInterviewProgress`). The FILES win on every disagreement (same posture as `department-floor.py`); the seam never writes a canonical artifact, never sets `interviewComplete`, never records a decision.
- **6 interview API routes + the answers route (`src/app/api/interview/turn`, `state`, `complete`, `answer`, `decision`, `canonical-departments`, `answers`).** The read/advance surface for the flow: `turn` and `answer` drive the conversational Q/A, `state` returns current phase/progress, `canonical-departments` serves the 25-department canonical set, `decision` records a provenanced department decision, `answers` lists the mirrored Q/A blocks, and `complete` runs the terminal closeout with a pre-flight that mirrors server **gates #2 (transcript) / #3 (coverage) / #8 (declines)** and fails closed with a structured 409 when a gate is unmet.
- **7-phase flow with a triple-gated Build button (`src/components/interview/*`, `src/app/interview/InterviewClient.tsx`).** Consent → structured question cards → conversational pane → progress rail → milestones → department decision board → review. The Build button in the review screen is triple-gated: it honors gate #2 (a real transcript exists), gate #3 (every department has a provenanced decision), and gate #8 (every decline is provenanced) before it will fire — the same three gates the `complete` route enforces server-side (defense in depth).
- **Migration 087 — interview read-mirror (`src/lib/db/migrations.ts`).** New idempotent migration adds `interview_sessions` + `interview_answers`, a **read-mirror / fast-UI index only**. These tables cache what the canonical files already say so the UI renders progress and an answer list without re-parsing on every request; neither is ever a write authority for `interviewComplete` or for the canonical reconciliation decisions. If the mirror and the files disagree, the files win.
- **Jargon lint (`scripts/lint-interview-jargon.ts`, `npm run lint:interview-jargon`).** A lint gate that keeps internal/operator jargon out of the client-facing interview copy.
- **Interview re-engagement nudge worker — DORMANT by default (`src/lib/jobs/interview-nudge-sweep.ts`, `src/lib/jobs/scheduler.ts`).** Re-engages an owner who started the interview and went quiet with a single Telegram message carrying a P0-7-contract resume link. Idempotent (one nudge per session/tier, highest crossed tier only), silent/operator-safe (owner chat only, never the operator, no Live Feed broadcast), and **opt-in**: a no-op unless `INTERVIEW_NUDGE_SWEEP_ENABLED=1` (repo-only until fleet rollout is released; disable entirely with `DISABLE_INTERVIEW_NUDGE_SWEEP=1`).
- **Seam-vs-Python parity harness (`src/lib/interview/__tests__/seam-parity.test.ts`, `__fixtures__/parity/*`, `scripts/seam-parity-golden.py`, `scripts/regen-seam-parity-golden.sh`).** A golden-fixture harness that proves the TypeScript seam readers produce byte-for-byte the same interpretation of the canonical files as the Skill-23 Python side, so the web surface can never drift from the box-side brain.
- **prove-zhe web end-to-end (`tests/e2e/prove-zhe-web.e2e.mjs`, `zhe-web-lib.mjs`, `zhe_gate_probes.py`, `ts-register.mjs`, `ts-resolve-hook.mjs`, `npm run test:prove-zhe-web`).** An e2e proof that drives the locked `/interview` surface through the gate probes end to end.
- **CI wiring (`.github/workflows/qc-cc.yml`).** The QC workflow gains the interview jargon lint + seam-parity + prove-zhe-web checks.

## [v4.59.6] — 2026-07-03 — chore(security): archive legacy root-level demo seed SQL out of the executable path (#15) — Big July 3rd Update Wave 4

Wave 4 of The Big July 3rd Update. Built by subagents, independently verified (fresh Opus, PASS on #15). Housekeeping/hardening only; no application logic/routes/schemas changed; repo is fleet-wide (no client names in any changed file); box user, not root.

- **#15 — legacy demo seed SQL archived off every code path (`seed-departments.sql`, `seed-departments-fixed.sql` → `docs/archive/legacy-demo-sql/`).** The two root-level demo `.sql` files — which an operator running them manually could use to inject ~17 fabricated demo tasks (e.g. "Define quarterly vision", "Approve marketing budget") plus a demo Live Feed event into a client database — are moved (pure `git mv`, content unchanged) into `docs/archive/legacy-demo-sql/`, where they sit off every automated path (migrations, `db:seed`, `db:reset`, `update.sh`). A new DO-NOT-RUN `README.md` in that folder documents why they are dangerous and points to the real, supported path.
- **`update.sh` Step 4 no longer suggests running them manually.** The old warning ("Skipping auto-apply — run manually if your schema has changed") is replaced with an explicit DEMO-ONLY / do-NOT-run-on-a-client-box warning that points at the app's automatic migration runner.
- **Real schema/data path unchanged.** The supported path — `src/lib/db/migrate.ts` → `runMigrations()`, migrations `085`/`086`, and `npm run db:seed` — is untouched; migration `086` still removes any legacy demo seed rows once a real company row exists. No migration, no schema change.

## [v4.59.5] — 2026-07-03 — chore(security): externalize the client roster out of the anti-leak gates (names, chat IDs, GHL location IDs off GitHub) + scrub residual client names — client-name hardening

Security/hardening pass. The real client roster (client names, Telegram chat IDs, GHL location IDs) no longer lives inline in the anti-leak gates — it is externalized to an operator-local, gitignored file and loaded at runtime, so client-identifying data is now 100% off GitHub while the gates still bite (fail-closed) in both modes. All additive; no application logic/routes/schemas changed; repo is fleet-wide; box user, not root.

- **Roster externalized out of the leak gates (`scripts/qc-assert-no-client-names.sh` v2.0.0, `scripts/qc-blocked-gate.sh`).** Both gates previously carried the real client denylist (names + numeric Telegram chat IDs + GHL location IDs) inline in tracked source. That denylist now lives in NO tracked file: it is loaded at runtime from an operator-local, gitignored roster resolved as `$OPENCLAW_CLIENT_ROSTER` → else `~/.openclaw/client-roster.txt`, via a new shared loader `scripts/client-roster-lib.sh`.
- **Two modes, never fail-open.** *Box mode* (roster present — operator box / pre-commit): scans every tracked file (and, for the diff gate, the diff) for every real roster name / chat ID / GHL ID — any hit FAILS (exit 1); this is the authoritative check. *Structural / CI mode* (roster absent, e.g. GitHub Actions without the secret): the gate does NOT silently pass — it still fails closed on the operator machine path and on any leaked obviously-fake `.example` placeholder name, and WARNS that the full roster-specific scan was skipped. Numeric chat-ID hits stay MASKED in output (last 4 digits only).
- **Committed placeholder template + shared loader.** `scripts/client-roster.example.txt` ships as the tracked template — obviously-fake placeholders only, no real data — for operators to copy to `~/.openclaw/client-roster.txt`; `scripts/client-roster-lib.sh` is the sourced loader (path resolution + name / chat-ID / placeholder accessors). `.gitignore` now excludes the repo-local roster copies so a filled-in roster can never be committed.
- **CI wiring (`.github/workflows/qc-cc.yml`).** The `no-client-names` job now (a) runs a hermetic BOX-mode self-test — points the gate at the committed `.example` roster and confirms the planted fixture still fails (exit 1), proving detection bites — then (b) runs the authoritative gate in structural mode against the real tree (operator-path + placeholder-leak scan, fail-closed).
- **Residual client names scrubbed.** Removed the last real client names from a code comment (`src/lib/campaigns.ts`) and a unit-test comment/assertion (`tests/unit/qc-gate-af-spell-spelling-fidelity.test.ts`), and re-based the planted CI fixture (`tests/fixtures/no-client-names/planted-client-name.txt`) onto a placeholder name so the self-test proves the detector without embedding any real name.
- **Result.** Client-identifying data (names, chat IDs, GHL location IDs) is now 100% off GitHub while the leak gates still fail closed in both box and structural/CI mode. Cross-repo consistent with the openclaw-onboarding roster externalization. No client names in any changed file; box user, not root.

## [v4.59.4] — 2026-07-03 — fix(build-status+sync): build-status route scans canonical company roots (#9 consumer), sync-departments no-over-prune (#11), slug-upsert UNIQUE crash (#13) — Big July 3rd Update Wave 3

Wave 3 of The Big July 3rd Update. Built by subagents, independently verified (fresh Opus, PASS on #9 consumer, #11, #13). All additive/idempotent; no client names in any changed file (repo is fleet-wide); box user, not root.

- **#9 (consumer) — build-status route now finds `build-progress.json` without `OPENCLAW_COMPANY_ROOT` exported (`src/app/api/onboarding/build-status/route.ts`).** `findActiveBuildProgress()` previously only scanned `OPENCLAW_COMPANY_ROOT` plus the legacy `/data/.openclaw/workspace/...` layout, so the `/onboarding/building` page showed no progress whenever that env var was not exported in the dashboard's process. It now also scans the canonical ZHC company roots the producer actually writes to — `/data/openclaw-master-files/zero-human-company` (VPS) and `~/Downloads/openclaw-master-files/zero-human-company` (Mac) — kept in sync with the `build-workforce.py` producer's `get_openclaw_paths()` resolution. Pairs with the onboarding **v17.0.12** producer so the building page reflects live build-progress on both platform layouts. No auth/route-shape change; purely an added candidate-root scan.
- **#11 — `sync-departments-from-build-state.py` no longer over-prunes valid rows.** The re-seed widened its existing-set to ALL workspaces (any `company_id`), so rows seeded under a stale `company_id` (e.g. the pre-064 `default`) are now **adopted** (re-homed to the real slug and refreshed) instead of being duplicated or crashing on the `PRIMARY KEY(id)` INSERT. Pruning of stale rows is **opt-in** via a new `--prune` flag (invoked only from `run-full-install` Phase 6c); ad-hoc syncs never delete. When pruning, a `RESERVED_WORKSPACE_IDS` allowlist (infra columns + CEO/master-orchestrator aliases, matched case-insensitively) and a **task-count guard** (any workspace still holding tasks is kept and logged for operator review) prevent deletion of any valid client row.
- **#13 — company upsert no longer crashes on a `UNIQUE(slug)` conflict.** `companies.slug` is UNIQUE, so the previous `INSERT ... ON CONFLICT(id)` raised a UNIQUE(slug) violation whenever an earlier seed had created the company under a different id (e.g. a uuid) with the same slug. The upsert now **queries by slug first** and branches UPDATE/INSERT, making it safe on every SQLite version (no dependency on multi-target `ON CONFLICT`, which needs SQLite ≥ 3.35).
- **Tests.** `tests/unit/test_sync_departments_from_build_state.py` extended to cover the widened existing-set + row adoption/re-homing, the opt-in `--prune` with reserved-allowlist and task-count guards, and the query-by-slug UPDATE/INSERT branch (no UNIQUE(slug) crash). No migration, no schema change.

## [v4.59.3] — 2026-07-03 — fix(seed): stop client-box demo pollution + duplicate master (P0 audit #1) + create sop_feedback table (#5) — Big July 3rd Update Wave 0

Wave 0 of The Big July 3rd Update (functioning-audit fixes). Built by subagents, independently verified (fresh Opus, PASS 5/5).
- **#1 (P0) — demo/duplicate-master pollution:** `seed.ts` now **guards the master Orchestrator insert** (SELECT `is_master=1 'Orchestrator'` first; reuse if present — re-runs can never create a second master) and makes **demo content opt-in** via `DEMO_SEED === 'true'` (default unset = **no demo** on client boxes; `SKIP_DEMO_SEED` removed). `db:push` is now **migrations-only** (new `src/lib/db/migrate.ts` → `getDb()`; no seed); `db:seed` stays the explicit guarded seed. New idempotent migration **086 `remove_demo_seed_rows`** de-dupes masters (keep oldest, FK re-point) and deletes legacy demo agents/tasks/"Team Chat" **only when a real company exists** — all try/catch-guarded, safe to re-run. `.env.example` updated.
- **#5 (P1) — dead SOP feedback loop:** new migration **085 `create_sop_feedback`** creates `sop_feedback(id, sop_id FK→sops, task_id, agent_id, rating INTEGER CHECK IN (1,-1,0), notes, created_at)` + indexes, matching `sop-learning.ts`/`/api/sops/feedback` exactly (revives the thumbs-rating endpoint + learning loop).

## [v4.59.2] — 2026-07-03 — docs(drift): reconcile Declared(verify) docs↔code — B.2 atomic-deploy shipped, Tailscale→Cloudflare Tunnel, /api/ws→SSE

Docs-only reconciliation from the 2026-07-03 feature-inventory "Declared (verify)" pass (one Opus 4.8 subagent, code-verified; **no application logic/routes/schemas changed**). Fixes: `docs/B2-atomic-deploy-spec.md` status "Scaffold — implementation pending" → "Implemented and shipped" (`scripts/atomic-deploy.sh` is the live 704-line implementation; QC gate 10.1 + `tests/unit/b2-atomic-deploy.test.ts` assert non-stub); `PRODUCTION_SETUP.md` + `docs/archive/HANDOFF.md` Tailscale references → Cloudflare Tunnel (platform is Cloudflare-Tunnel only); `docs/REALTIME_SPEC.md` `/api/ws` WebSocket sections annotated **NOT SHIPPED**, pointed to the live SSE transport `/api/events/stream`. Two inventory signals proved WRONG on verification and were left as-is (both genuinely ship): the CEO-board "Queued" column and Devil's Advocate / `da_challenges` — nothing was built. **Department-count drift deferred:** 12 conflicting count literals (code ground truth = **25 canonical departments** per `departments.config.ts` / `qc-cc.sh` check 2.5 / `intelligent-routing.test.ts`; docs lag at 16/18/24) are documented but intentionally NOT changed here — canonicalization is owned by the in-flight department-floor audit so we don't force a wrong floor. The onboarding `cc-compat` pin intentionally stays at **v4.59.1** (docs-only; no functional change for clients).

## [v4.59.1] — 2026-07-03 — fix(status-contract): reconcile presentations terminal-status contract (drop orphan 'delivered', single 'done'+cert terminal)

Reconciles an internal contract inconsistency surfaced by the July-3 verification pass. `src/lib/validation.ts` `UpdateTaskSchema` TaskStatus enum has no `'delivered'` (a PATCH carrying it 400s at the schema), yet `src/lib/presentations-cert-gate.ts` listed `'delivered'` as a terminal status — the two files disagreed, so a presentations card could never reach a terminal closed state through the contract. The onboarding presentations rail now closes a completed deck via `status='done'` + a matching `process_certificate_sha` (`'delivered'` is a note, not a status). Fix: removed the orphan `'delivered'` from the cert-gate terminal set (now `{'done'}`); exported `TaskStatus` (values unchanged, 10) + `PRESENTATIONS_TERMINAL_STATUSES` for a new contract test `tests/unit/presentations-cert-contract.test.ts` that pins terminal-statuses ⊆ the schema enum, accepts `done`+valid-cert, and rejects `delivered`. The anti-spoof stored-cert MATCH check is untouched (no gate weakened). Additive/idempotent; no client names (repo is fleet-wide); box user, not root.

## [v4.59.0] — 2026-07-01 — fix(final-review-hardening): persona-exhaustion department-default pin, QC provider-down deferral, backlog-redispatch escalation cap

Closes three board-liveness gaps surfaced during the final review pass. All additive/idempotent; no client names in any changed file (repo is fleet-wide); box user, not root.

- **Persona exhaustion now pins a deterministic department-default (`src/lib/tasks.ts`, migration 083).** `resolvePersonaAndPin()` previously left a task personaless after `PERSONA_PIN_MAX_ATTEMPTS` was exhausted. It now falls back to a deterministic department-default persona and pins it, marking the pin `persona_fallback=1` (new `tasks.persona_fallback` audit column, migration 083, idempotent `ALTER TABLE`) plus a queryable audit event, so the board invariant — every task carries a persona — holds even under total selector exhaustion. A genuinely-matched persona is unaffected; only the exhaustion path sets the flag. `tests/unit/point10-persona-exhaustion-fallback.test.ts` covers the fallback selection, the pin, and the audit flag/event.
- **QC provider-down deferral (`src/lib/qc-scorer.ts`, `src/lib/jobs/qc-review-sweep.ts`).** The heuristic-fallback QC path already distinguished `'no-key'` from `'provider-down'` internally but both landed in ordinary human review. A `'provider-down'` result (a key IS configured but every LLM call failed — outage/network blip) now writes a `[QC-DEFERRED-PROVIDER-DOWN]` marker instead and is held in review rather than surfaced as a human-actionable item; `qc-review-sweep` excludes it from the human-required queue and auto-rescores it on its next pass once the provider recovers, so a transient provider blip can no longer trigger a human-review storm. `'no-key'` (genuinely unconfigured) is unaffected and still routes to normal review. `tests/unit/point6-qc-provider-down-defer.test.ts` covers marker placement, sweep exclusion, and auto-rescore on recovery.
- **Backlog-redispatch escalation cap (`src/lib/jobs/backlog-redispatch-sweep.ts`, migration 084).** Mirrors the existing `QC_MAX_REROUTES` pattern: a task stuck in the cheap re-dispatch loop (config problem or an unresolved hold that never reaches `recordDispatchFailure`) is now escalated to `status='blocked'` once it has both exceeded `REDISPATCH_MAX_ATTEMPTS` (K=20, env-overridable) retries AND been stuck for at least `REDISPATCH_ESCALATE_HOURS` (M=6h, env-overridable) — closing the last permanent-reloop path. New `tasks.redispatch_count` column (migration 084, idempotent `ALTER TABLE`) tracks the cheap-retry count per task. Escalation writes a `[REDISPATCH-CAP]` note with `block_audience='SYSTEM'` (operator feed only — no client Telegram, per the silent-updates doctrine) so a config-level stall reaches the operator, not the client. `tests/unit/point6-backlog-redispatch-cap.test.ts` covers the K-and-M cap logic and the SYSTEM-audience block note.
- **Migrations 083 / 084** are sequential (latest before this was 082), idempotent (`PRAGMA table_info` guard before `ALTER TABLE ADD COLUMN`), additive, and run on boot. No schema regression to any already-shipped column.
- No client names in any changed file. Box user, not root.

## [v4.58.0] — 2026-07-01 — fix(release-hygiene): repair-script self-heal restored, no-client-names/shell-syntax CI gates, committed roster purge, platform-safe dispatch fallback, sync-departments canonical-path resolution, model-sovereignty parity, status-route auth hardening, migration-058 index fix, vitest/node:test glob fix

Consolidates a batch of release-hygiene and hardening fixes surfaced during the repo review sprint. All additive/idempotent; no client names in any changed file (repo is fleet-wide); box user, not root.

- **`scripts/repair-command-center.sh` syntax fix — self-heal un-killed.** A shell syntax error was short-circuiting the self-heal before it reached step (a), the `better-sqlite3` native-module ABI rebuild (`npm rebuild better-sqlite3`, followed by a load-and-query smoke check confirming the module's compiled ABI matches the running Node ABI). `bash -n scripts/repair-command-center.sh` now parses clean and the self-heal runs end-to-end again.
- **Tracked-file client-data gate — `scripts/qc-assert-no-client-names.sh` v1.1.0.** Adds a NUMERIC client Telegram chat-ID denylist class (PRD P0-3 follow-up) alongside the existing name/keyword scan, with a masked-output convention so a caught offender's value is never echoed in full to CI logs. This closes the gap in `qc-blocked-gate.sh` Assertion 5, which only scans the git diff and never catches a client name that was already committed — this gate walks every tracked file on disk instead.
- **New CI jobs (`.github/workflows/qc-cc.yml`).** **`no-client-names`** — runs a hermetic self-test (a planted client-name fixture must fail the gate) before running the authoritative tracked-file scan against the real repo. **`shell-syntax`** — runs `bash -n` over every tracked `*.sh` file (root `scripts/`, `scripts/install/`, `scripts/cloudflare/`, `scripts/integration-tests/`, `scripts/conversational-ai/`, `fleet-heartbeat/scripts/`, `.claude/`) so a broken heredoc or quoting error can never ship again — the same class of bug this release's repair-script fix corrected.
- **Committed client/operator data purged.** The fleet roster (client box labels, IPs, container names, SSH aliases) is no longer tracked in this fleet-wide repo. `fleet-heartbeat/scripts/propagate-rescue-webhook.sh` now loads it at runtime from operator-local files (`RESCUE_VPS_ROSTER_FILE` / `RESCUE_MAC_ROSTER_FILE`, defaulting under the operator's own accounts directory), warning loudly instead of silently no-op'ing when a roster file is absent or empty.
- **Dispatch runtime — platform detection replaces the hardcoded home fallback (`src/lib/task-dispatcher.ts`, P1-5).** The specialist-runtime path resolver no longer falls back to a hardcoded operator home directory; it now resolves via `process.env.HOME || process.env.USERPROFILE || os.homedir()` so a box where `HOME` is unset (PM2/systemd/container contexts) still resolves correctly. New no-literal test `tests/unit/p1-5-no-hardcoded-operator-home.test.ts` asserts the source contains no hardcoded absolute home path.
- **`scripts/sync-departments-from-build-state.py` canonical-path resolution.** Now resolves the canonical master-files roots, the VPS DB path, and `companySlug` correctly instead of assuming a single fixed layout, so the sync succeeds across Mac-mini and VPS installs alike. 11 new pytest cases cover the resolution paths.
- **Model sovereignty.** `FORBIDDEN_PREFIXES` (`src/lib/model-selector.ts`) is kept at full parity with the onboarding Python selector's forbidden-prefix list. The Anthropic connector (`src/lib/model-providers/index.ts`) is gated behind `ALLOW_ANTHROPIC_PROVIDER` (default OFF) so it is never available to selection on a client box unless an operator explicitly opts in. A demo-seed fixture's slug that referenced a specific value was swapped for a generic placeholder.
- **Status route auth hardening (`src/app/api/tasks/[id]/status/route.ts`).** The `WEBHOOK_SECRET` HMAC middleware gate now covers this dynamic route the same way it already covers `/api/tasks/ingest`. `'done'` is rejected with 403 unconditionally, regardless of auth. `'blocked'` is scoped: a signed caller may only move a card into `blocked` when it is Skill-6-marked, closing the gap where any signed caller could force-block an arbitrary card. 9 new pinning tests cover the auth/scope matrix.
- **Migration 058 — `idx_tasks_archived_at` index now created unconditionally.** The `CREATE INDEX IF NOT EXISTS` was previously nested inside the column-absence guard, so a fresh install (where `schema.ts` already declares `archived_at`) skipped the `ALTER TABLE` branch and never got the index. The index create now runs outside that guard on every DB, old or new — idempotent either way.
- **`vitest.config.ts` / Node `:test` glob collision fixed.** `vitest run` no longer picks up test files intended for the Node built-in test runner (`npm run test:unit` via `tsx --test`), eliminating spurious "no test suite found" failures between the two suites.
- No migration schema regression to any already-shipped column. No client names in any changed file.

## [v4.57.0] — 2026-07-01 — feat: task status-transition consumer endpoint (Skill-6 Kanban)

- **New consumer endpoint `POST /api/tasks/[id]/status` (`src/app/api/tasks/[id]/status/route.ts`).** Applies a board task's status transition delivered by the `cc_board.py` producer. Authenticates via `Authorization: Bearer <MC_API_TOKEN>` and verifies `x-webhook-signature` = HMAC-SHA256(raw body, `WEBHOOK_SECRET`) as lowercase hex — both constant-time. On a valid transition it mutates the task status, emits a `task_status_changed` event, and writes a `task_history` row; rejects bad signature/token (401), unknown id (404), invalid status (400).
- **Verified end-to-end** against the already-committed `cc_board.py` producer: producer HMAC equals consumer HMAC over identical raw bytes; a tampered body is rejected (401) while the untampered control passes (200). Unit suite `tests/unit/task-status-transition.test.ts` (5/5) + `tsc --noEmit` clean.
- No migration, no schema change. Additive. No client names in changed files.

## [v4.56.2] — 2026-06-29 — fix: FM-6 dedupe welcome task rows + correct presentations model_id env-default path

- **FM-6 dedupe fix — `src/app/api/departments/route.ts`:** `createDepartmentInDbDirect` had no idempotency on the "Welcome to …" starter-task INSERT. Every call that bypassed the outer workspace-exists check (parallel requests, external scripts, aborted transactions) could insert a second row with the same title and workspace_id. Added a check-before-insert guard inside the transaction: `SELECT id FROM tasks WHERE workspace_id = ? AND title = ? LIMIT 1` — if a row already exists, its id is reused and the INSERT is skipped. Minimal, schema-free, consistent with the codebase's existing check-before-insert pattern.
- **FM-6c env-default gap fix — `src/lib/model-selector.ts`:** `resolveSovereignDefault()` checked `checkModelSovereignty()` for the `SOVEREIGN_DEFAULT_MODEL` env var, but that function skips the modality check when `required_modality === 'text'` (it only validates wrong-modality for non-text tasks). A TTS model set as the env default therefore passed sovereignty and was returned as the reasoning `model_id` for any text task — including presentations tasks. Added the same `canServeTextTask()` guard used by the eligible-inventory scan: if `required_modality === 'text'` and the env-default model is a pure TTS/media model, the env default is skipped and the inventory scan runs. No change to behavior for non-text modalities or for language-model env defaults.
- No migration, no schema change. No client names in changed files. Additive + idempotent.

## [v4.56.1] — 2026-06-29 — feat: Presentations board-resilience completion — FIX C v2 (matching cert), SOP-furnace reaper, model_id TTS fix, dispatcher canonical-runtime, department dedup (FM-6)

Completes the Command-Center half of the Presentation-department remediation, building ON v4.56.0 (#118, which shipped the ingest self-heal + the v1 presence-only done-gate). This release does NOT regress #118: its FIX J (`ingest/route.ts` schema self-heal + 503 + `notifyOwnerSchemaError`) and `validation.ts` field are kept as-is; the done-gate enforcement is preserved and STRENGTHENED. All items additive + idempotent; no client names in any changed file (repo is fleet-wide); box user, not root. The [ALREADY SHIPPED] pieces (`tasks.sop_id` + migration 056 + backfill 053; `resolveWorkspaceId` no-bare-`default`; `isWorkforceProvisioned` gate) were re-verified present and left UNCHANGED.

- **FIX C v2 — deck no-skip proof now MATCHED, not just present (`src/app/api/tasks/[id]/route.ts`, `src/lib/presentations-cert-gate.ts`, migration 080).** v4.56.0 enforced a non-empty `process_certificate_sha` and explicitly deferred "verification against a stored certificate hash" to v2 — this delivers it. New `tasks.process_certificate_sha` column (migration 080, additive/nullable) stores the registered cert. The pure, unit-tested `evaluatePresentationsDoneGate()` decides: none stored + none presented → reject (still required, no regression); a registered cert → the mover MUST present the SAME sha (anti-spoof) else reject; a valid sha presented with none stored → accept + persist as the certificate of record. Rejections keep v4.56.0's `422` + `requires_process_certificate` response shape (now also carrying `code` + `remediation`). Scoped to `presentations`; other departments unaffected.
- **FM-6b — SOP-authoring furnace stopped (`src/lib/sop-authoring.ts`, `src/lib/db/task-dedup.ts`, migration 082).** The dispatch sweep re-created an identical open `Author SOP: …` sub-task every ~2 min (hundreds stuck `in_progress`). A runtime IDEMPOTENCY GUARD reuses an existing open authoring sub-task (matched on the original-task link OR title+department) and returns a new `deduped` status instead of inserting another; migration 082 + the exported `reapDuplicateOpenAuthoringTasks()` FK-safely collapse pre-existing clones (keep the oldest). The existing W8.2 attempt cap already hard-stops `no_specialist_runtime` redispatch after `MAX_DISPATCH_ATTEMPTS`.
- **FM-6c — model_id no longer populated from the wrong field (`src/lib/model-selector.ts`).** A pure media/TTS model (e.g. `gpt-4o-mini-tts`, capability `audio_generation` only) could win a TEXT task because the modality filter accepted ANY model for `text`. A new `canServeTextTask()` predicate excludes pure media-output/embeddings models from text selection in BOTH `selectTaskModel()` and the `resolveSovereignDefault()` Layer-4b fallback, so a presentations/text task can never pin a TTS model as its reasoning `model_id`.
- **Dispatcher legacy-slug → runtime (`src/lib/task-dispatcher.ts`).** `resolveSpecialistSessionKey()` now probes the CANONICAL runtime dir (`canonicalDeptSlug`) for an aliased workspace slug (`ceo` → `dept-master-orchestrator`, `app-development` → `engineering`) before reporting `no_specialist_runtime`, so an aliased department DISPATCHES instead of looping in backoff. Dispatch is NOT gated on a NULL `agents.model` — the resolver's sovereign-default + the W8.2 cap already fall back to the box's configured model or surface ONE clear wiring error (re-verified; no infinite refuse-loop).
- **Department single-source / dedup (`src/lib/db/task-dedup.ts`, migration 081, seeding-path guards).** Duplicate workspace rows that canonicalize to ONE department (`ceo` + `master-orchestrator`, `app-development` + `engineering`) are merged to the canonical keeper (`dedupeCanonicalWorkspaces`, migration 081), reassigning every `workspace_id`-bearing row before deleting the loser. Direction follows the repo's single source of truth `canonicalDeptSlug()` (the 2026-06-28 UNIT-ENG promotion made `engineering` canonical) so the board's list stays consistent with routing — merging the other way would orphan routed tasks. A slug-uniqueness guard (`findCanonicalWorkspaceId`) at every seeding path (`autoSeedFromDepartmentsJson`, `reseedWorkspacesFromConfig`, the `createDepartmentInDbDirect` operator override) prevents re-creating a second row for an existing department.
- **Migrations 080 / 081 / 082** are sequential (latest before this was 079; #118 added none), idempotent, and run on boot. **No schema regression** to any [ALREADY SHIPPED] column.
- **Guards/tests (`tests/unit/`, Node test runner, isolated throwaway DBs):** `presentations-cert-gate` (done-gate requires + anti-spoofs the cert), `model-selector-text-serviceability` (TTS never selected for text; NULL-model sovereign-default still resolves), `cc-board-dedup-reaper` (no duplicate department slugs; reaper idempotent — no duplicate open tasks; legacy slug → canonical runtime; migration-080 column present), `sop-authoring-idempotency` (open authoring sub-task reused, none re-created).
- **Independent verification:** `tsc --noEmit`, `scripts/qc-cc.sh` (111 green), and `npm run build` pass; every existing unit suite in the touched areas (model-sovereignty, fast-loop-QC, owner-notify, trio-seed, workspace-FK, ingest-dedup, routing) passes in isolation; a stash-to-`main` baseline confirmed the bulk-run flakiness set is pre-existing and unchanged by this PR.

## [v4.56.0]  —  2026-06-29  —  feat(resilience+presentations): ingest schema-error self-heal + owner escalation; presentations done-gate (no-skip proof at the board)

Two additive, low-risk hardening items for the task capture pipeline and the presentations department gate.

**EDIT 1 — Ingest catch block: schema-error self-heal + clear 503 + owner escalation (`src/app/api/tasks/ingest/route.ts`)**

The existing catch block returned a blind `500 Internal server error` with no distinction between a generic crash and a schema problem. The canonical failure mode is a box whose migrations have not yet run or ran partially (e.g. `SQLITE_ERROR: table tasks has no column named sop_id` — the historical migration 056 case, or `stage_slug` for 079). The new block:

- Detects schema errors via `/SqliteError|no column named|no such column|no such table/i`.
- Attempts a one-shot self-heal: `runMigrations(getDb())` runs pending migrations in-process so future requests succeed (best-effort, own try/catch).
- Escalates: calls `notifyOwnerSchemaError(msg)` (best-effort, gateway-routed via `notifyOwner`) so the owner is told the box needs `npm run db:seed` / a redeploy.
- Returns a CLEAR 503 with `{ error, detail, remediation, schema_error: true }` instead of an opaque 500. The caller can distinguish a schema problem and retry.
- BUILD-SAFE: task-field variables (`title`, `finalDescription`, etc.) are declared inside the try block and are NOT in scope in the catch — no `createTaskCore` retry is attempted. The self-heal brings the schema current for the next request; the caller must retry.
- Non-schema failures still return a 500 with `detail: msg` added.

New imports: `getDb` added to the existing `@/lib/db` import; `runMigrations` from `@/lib/db/migrations`; `notifyOwnerSchemaError` added to the existing `@/lib/owner-reports` import.

**EDIT 1b — `notifyOwnerSchemaError` (`src/lib/owner-reports.ts`)**

New export, modeled on `notifyOwnerAssigned`. Gateway-routed via `notifyOwner`; fail-soft; returns boolean; never throws into the request path.

**Presentations done-gate — `process_certificate_sha` required at the board (`src/app/api/tasks/[id]/route.ts`, `src/lib/validation.ts`)**

Any PATCH that transitions a task whose department canonical-slug is `presentations` to `status: 'done'` now REQUIRES a non-empty `process_certificate_sha` in the request body. If missing or empty the endpoint rejects with 422 `{ error: '...', requires_process_certificate: true }` before the status commit — the board cannot be marked done without proof the no-skip floor script ran. Other departments are completely unaffected (slug check is scoped to `presentations` only; the gate is inside the existing status-transition block after the Triad gate and before `updates.push('status = ?')`).

`UpdateTaskSchema` in `src/lib/validation.ts` gains an optional `process_certificate_sha: z.string().optional()` field so the Zod parse accepts the field from the body; the API route enforces presence for presentations specifically.

v1 note: presence of the hash is the gate; verification of the hash value against a stored certificate is a planned v2 addition. This ships the board-side enforcement that prevents any path — agent or human drag — from closing a presentations task without the prove-deck.py proof token.

- No migration, no schema column change. No client names. Box user, not root.
- **PROOF** — `npm run build` (next build / typecheck) green; `npx vitest run` green. No client names in changed files.

## [v4.55.4] — 2026-06-29 — fix(pm2): reconcile the WHOLE repo to the fleet-canonical app name `blackceo-command-center` (supersedes v4.55.3's interim direction)

The fleet-canonical pm2 app name is **`blackceo-command-center`** — the name the openclaw-onboarding installer, every per-box fleet dedup, and `scripts/deploy.sh` standardize on. Earlier revisions of THIS repo diverged to `mission-control` across `ecosystem.config.cjs`, the `scripts/install/*-bootstrap.sh` ecosystem templates, `scripts/deploy.sh`, and `scripts/watchdog-cc.sh` self-heal. On a box already running `blackceo-command-center`, that divergence meant `pm2 resurrect` / a bootstrap re-run / a watchdog self-heal would (re)create a SECOND app named `mission-control` on :4000 — the two-process mutual-kill loop that wedged a client box. (v4.55.3 documented the contract but pointed the comment at the wrong name; this release corrects every file.)

- **`ecosystem.config.cjs`** — `name` is now `blackceo-command-center` (was `mission-control`). This is what `pm2 resurrect` restores at boot and what `pm2 start ecosystem.config.cjs` (watchdog self-heal, INSTALL guidance) creates, so it can no longer spawn a rival. The on-disk `DATABASE_PATH` filename stays `mission-control.db` — that is a file path, NOT the app name.
- **`scripts/install/mac-mini-bootstrap.sh` + `scripts/install/vps-docker-bootstrap.sh`** — the written ecosystem template `name` is now `blackceo-command-center`, and the idempotent-heal reconcile now treats a stale `"mission-control"` (and the older bare `"command-center"`) as needing-update, so a re-bootstrap converges an old box to the canonical name.
- **`scripts/deploy.sh`** — `PM2_NAME="blackceo-command-center"`.
- **`scripts/watchdog-cc.sh`** — self-heal still clears EVERY CC app name (canonical + legacy) and restarts via `pm2 start ecosystem.config.cjs`; because ecosystem now declares `blackceo-command-center`, the end state is always exactly one canonical CC and the legacy `mission-control` is never resurrected. Misleading "legacy" comments corrected.
- **`scripts/cc-start.sh`** — the v4.55.3 orphan-port-killer self-pid safety (`$$`/`$PPID` exclusion) is retained; its comment now names the correct canonical.
- **Docs** — `DEPLOYMENT.md` and `docs/OPENCLAW_BRIDGE_PAIRING.md` pm2 command references reconciled to `blackceo-command-center` (DB-file / install-dir references left as-is).
- **PROOF** — drove the REAL `scripts/watchdog-cc.sh` self-heal + `pm2 start ecosystem.config.cjs` against a box already running `blackceo-command-center`: a zombie `{blackceo-command-center, mission-control}` converges to exactly one `blackceo-command-center` (mission-control retired, never resurrected); a healthy box is untouched; the ecosystem start spawns no duplicate.
- Note: `package.json` `name` ("mission-control") is the npm package identity, NOT a pm2 app name, and is intentionally left unchanged to avoid package-lock / npm-ci churn. No migration, no schema change, no config/template change. Box user, not root. No client names in changed files.

## [v4.55.3] — 2026-06-29 — fix(pm2): document the fleet-canonical app name + harden cc-start.sh orphan-port killer against self/supervisor kills (two-CC-on-:4000 defence-in-depth)

- **Context — the root cause (fixed in the onboarding installer, hardened here).** A box could end up running the Command Center under TWO pm2 app names at once: `mission-control` (what `ecosystem.config.cjs`, `pm2 resurrect`, `scripts/watchdog-cc.sh`, and the `scripts/install/*-bootstrap.sh` scripts all use) and the divergent `blackceo-command-center` that the openclaw-onboarding installer historically started — with BOTH bound to :4000. The two `next start` children then mutually killed each other through cc-start.sh's orphan-port killer: an endless restart loop (47k+ restarts) that wedged the gateway on a client box. The app-name unification lands in the installer (openclaw-onboarding v16.1.6, Phase 6 reconcile + start-from-ecosystem); this release closes the two remaining CC-repo gaps.
- **`ecosystem.config.cjs` — canonical-name contract documented in-code.** Added a comment above `name: 'mission-control'` declaring it the SINGLE fleet-canonical pm2 app name and listing every tool that must agree (this config, watchdog-cc.sh, the bootstrap scripts, package.json, the installer Phase 6). The value is unchanged — `mission-control` was already canonical — so there is no behavioural change and the port-pin / circuit-breaker guards (qc-cc.sh §11.1–§11.9) still hold.
- **`scripts/cc-start.sh` — orphan-port killer self-pid safety.** `free_port()` now strips this launcher's own pid (`$$`) and its pm2/npm supervisor (`$PPID`) from the kill list via a new `_cc_strip_protected` helper, applied to the initial port scan, the post-TERM re-probe, and the final check. With one canonical pm2 app there is no longer a sibling CC to fight, so the killer can no longer cause the mutual-kill; the pid exclusion is defence-in-depth so cc-start.sh can never SIGTERM the process tree that supervises the canonical CC. Any LISTENer remaining after the exclusion is, by definition, a stale orphan from a prior boot/restart and is still safely reclaimed. The env-bleed strip (`unset PORT`, qc-cc.sh §11.4) and the lsof/fuser orphan detection (§11.5) are unchanged.
- No migration, no schema change, no config/template change. Box user, not root. No client names in changed files.

## [v4.55.2] — 2026-06-28 — fix(db): self-heal `tasks.stage_slug` (migration-ID-074 collision) so ad-campaign card-create stops 500ing

- Migration 079 (`ensure_tasks_stage_slug`, idempotent `PRAGMA table_info` guard + `CREATE INDEX IF NOT EXISTS` ×2): `ALTER TABLE tasks ADD COLUMN stage_slug TEXT` plus the two ad-campaign indexes. Mirrors migration 074's body exactly; additive + nullable, never touches existing rows or routes.
- **Root cause:** the migration runner keys applied-state on `id` alone (`_migrations.id`). An earlier source revision numbered a since-removed migration as id `074`; current source assigns id `074` to `add_tasks_stage_slug_for_ad_campaigns`. Any DB that recorded the OLD `074` silently SKIPS the current `074` forever, so `tasks.stage_slug` is never created on those DBs.
- **Symptom (same class as migration 056 `sop_id` and 078 `block_reason`):** the ad-campaign assembly-line INSERT in `src/lib/ad-campaigns.ts` writes `stage_slug`, so on an affected DB it throws `SQLITE_ERROR: table tasks has no column named stage_slug` and the ad-run card-create path returns HTTP 500. Reproduced and verified against a live DB carrying the `074` collision.
- A NEW migration id (`079`) is used so it always runs on affected DBs; idempotent so it is a no-op where `074` already added the column. `schema.ts` already declares `stage_slug` in the base `CREATE TABLE tasks`, so fresh installs were never affected.
- **Audit note:** the canonical task ingest/create path (`createTaskCore`, used by `POST /api/tasks` and `POST /api/tasks/ingest`) writes `sop_id`, which is already covered by migration 056 + `schema.ts`; a full column-drift audit confirmed `stage_slug` was the only code-written `tasks` column missing on a live DB.

## [v4.55.1] — 2026-06-28 — fix(W8): add block_reason column so gateway-down/stuck tasks actually transition to blocked + notify owner

- Migration 078 (idempotent `PRAGMA table_info` guard): `ALTER TABLE tasks ADD COLUMN block_reason TEXT`.
- Without this column the block-on-N UPDATE in `recordDispatchFailure` (task-dispatcher.ts) and the QC-BLOCKED UPDATE in `qc-scorer.ts` threw SQLITE_ERROR "no such column: block_reason"; the throw was caught and swallowed, leaving tasks stuck in backoff forever and owner notifications never firing.
- After this migration: a task reaching `MAX_DISPATCH_ATTEMPTS` correctly transitions to `status='blocked'`, increments `dispatch_attempts` to the cap, sets `next_dispatch_eligible_at = NULL`, and fires `notifyOwner`. The re-loop-on-backoff path is structurally eliminated.
- Removed stale `.bak` siblings under `src/lib/` and `src/app/api/`.

## [v4.55.0] — 2026-06-28 — feat: W8 board-liveness + furnace-safe guards, W4 full-context handoff, W5 owner notifications, W3 routing gate + general fallback + owner-direct

Ships the four Work items that close the "nothing sticks" board-liveness failure:
**W8** (board-liveness + furnace-safe advancement), **W4** (full-context handoff), **W5** (owner notifications), and **W3** (routing gate + general fallback + owner-direct specialist pin).

**W8 — Board-liveness + furnace-safe guards (migration 077, new job, model-selector, sweeps):**
- Migration 077 adds `dispatch_attempts`, `last_dispatch_attempt_at`, `next_dispatch_eligible_at` columns to `tasks` (idempotent `ALTER TABLE` + partial index). Enables per-task backoff accounting without touching any existing row.
- `src/lib/jobs/intake-advance-sweep.ts` (new): THE single board-advancement authority. Every 2 min it drains every intake lane (`inbox`/`backlog`/`planning`/`pending_dispatch`/`assigned`): routes unassigned tasks, feeds campaign boards (W8.4), and fires `autoDispatchTask`. Furnace-proof by construction: selects only tasks under the QC re-route cap, under the dispatch-attempt cap, and past their exponential-backoff window — so an unadvanceable task drops out instead of being re-fired forever. Registered in scheduler JOBS[] as `intake-advance`. Disable with `INTAKE_ADVANCE_SWEEP_ENABLED=0`.
- `src/lib/task-dispatcher.ts`: `recordDispatchFailure()` + `recordDispatchSuccess()` helpers stamp attempt-accounting on every failed/successful advance, backing off exponentially (default 120 s base, 2× per attempt, cap 3600 s). After `MAX_DISPATCH_ATTEMPTS` (default 5) the task is hard-blocked (visible on board) and the owner is notified — silent re-looping is structurally impossible. Guard 6 (backoff window check) added to `autoDispatchTask` so even mid-tick re-selections are cheap no-ops.
- `src/lib/model-selector.ts`: `resolveSovereignDefault()` (new) guarantees a non-null, sovereign, modality-fit model as the dispatch last resort, eliminating the 172/183 null-`model_id` root cause. Respects `SOVEREIGN_DEFAULT_MODEL` env, then walks the client's inventory tier 1→2→3. Never substitutes an owner's express model.
- `src/lib/jobs/backlog-redispatch-sweep.ts` + `src/lib/jobs/ceo-delegation-sweep.ts`: Both sweeps now honour the `dispatch_attempts` cap and `next_dispatch_eligible_at` backoff window in their SELECT query — a blocked or backed-off task cannot re-enter the fire queue from either sweep either. Both sweeps remain PAUSED (`*_ENABLED=0`) pending `intake-advance` verification.
- `src/lib/campaigns.ts` (new): `ensureCampaignForTask()` — creates or attaches a task to its department's live campaign board (W8.4). Called by `intake-advance-sweep`.

**W4 — Full-context handoff (`src/lib/context-pack.ts` new, `src/lib/task-dispatcher.ts`):**
- `src/lib/context-pack.ts` (new): Assembles a typed `ContextPack` for every dispatched task. Includes task-targeted excerpts of the receiving specialist's core files (`AGENTS.md`/`TOOLS.md`/`MEMORY.md`), pointer references to `how-to-use-this-department.md`, OpenClaw master files, the SOP, and per-domain research sources. Also threads optional `context_refs` from the ingest payload (W4.1) so the CEO can pass explicit doc pointers.
- `autoDispatchTask` splices the rendered context pack into the task message before dispatch — the receiving specialist never starts blind.

**W5 — Owner notifications (`src/lib/owner-reports.ts` new, dispatcher + ingest):**
- `src/lib/owner-reports.ts` (new): Thin funnel for the three owner messages spec §5 requires: `notifyOwnerAssigned` ("I'm sending this to [Dept]"), `notifyOwnerStarted` (persona + dept + specialist + SOP + role), `notifyOwnerDone` (who + where-to-find-it). Each helper self-resolves all fields from the live DB given only a `taskId`. `notifyOwnerHeld` covers the held-task edge case.
- `notifyOwnerAssigned` called from `/api/tasks/ingest` on every successful routing result.
- `notifyOwnerStarted` called from `autoDispatchTask` immediately after dispatch write.
- `recordDispatchFailure` calls `notifyOwner` when a task hits the block cap.

**W3 — Routing gate + general fallback + owner-direct specialist pin:**
- `/api/tasks/ingest`: `isWorkforceProvisioned()` gate (W3.1) — routing applies only to boxes with a completed Zero Human Company interview AND materialized non-shell departments (spec §3). Shell/legacy boxes are exempt; tasks are still captured but not forced through department classification. Fail-safe: when the self-client row is absent, the gate defers to the materialized-departments signal alone.
- `WEBHOOK_SECRET` now required in production (W3.5): ingest returns 503 when unset in `NODE_ENV=production` instead of silently accepting unauthenticated writes.
- `src/lib/routing/department-router.ts`: `resolveSpecialistPin()` (new, W3.2) — owner-named specialist bypass. When the owner names a specific AI, the CEO routes STRAIGHT to it (id → exact name → exact persona → unique substring), bypassing `pickBestAgent` and department classification entirely. Falls through to normal routing when the named agent is not found. Wired as Step 0 in `comDispatch`.
- Ingest payload extended: `target_agent` / `specialist` accepted as specialist-pin aliases; `context_refs` for doc-pointer references.
- `src/lib/routing/canonical-slug.ts`: General fallback slug added — tasks with no resolvable department now fall into the `general-task` workspace rather than being dropped.

**NOTE: The furnace flags (`BACKLOG_REDISPATCH_SWEEP_ENABLED`, `CEO_DELEGATION_SWEEP_ENABLED`) REMAIN set to 0. Re-enable is a separate verified step once the `intake-advance` worker is confirmed draining the board.**

**Migrations note:** Migration 077 is idempotent (`PRAGMA table_info` guards on each `ALTER TABLE`; `CREATE INDEX IF NOT EXISTS`). Safe to apply to the live mission-control.db via a controlled `npm run db:migrate` — no destructive SQL. The live DB was NOT touched during this PR.

## [v4.54.0] — 2026-06-27 — feat: Command Center harmony — config template, embedding-contract pin, config-guard CI, resolver reorder

Consolidates the Command Center "harmony" fixes so a freshly-built box renders the REAL departments, never the 17-demo defaults, and the SOP semantic-suggest store uses a single canonical embedding contract.

**Config template (no demo/client data on main):**
- `config/departments.json` emptied to `[]` and `config/company-config.json` reset to the generic "Your Company" template with `departments: []`, fixing the v4.0.3 regression that resurrected 17 demo departments. (PR #109)

**Departments-path resolver reorder (`src/lib/db/migrations.ts`):**
- `resolveDepartmentsConfigPath()` now probes the newest real ZHC company build on disk BEFORE the repo-committed `config/departments.json` template, so a built client never seeds the generic 17-demo defaults.

**Embedding contract pin + backfill (`src/lib/sop-embeddings.ts`, `scripts/backfill-sop-embeddings.ts`):**
- Pin Google (gemini-embedding) as the single embedding contract; the backfill script now PURGES any non-active-model rows so only one canonical embedding per SOP remains.
- Added `db:embed:sops` npm script.

**Cross-store guard CI (`.github/workflows/config-guard.yml`, `scripts/qc-cc.sh`):**
- New config-guard workflow blocks demo/client data from shipping on main; qc-cc.sh gains the cross-store embedding-contract and config-template checks.

## [v4.53.2] — 2026-06-27 — fix: verified low-severity hygiene (FilePreview useEffect, chart guards, dead imports, LIKE escaping, any-types)

Applies five verified low-severity hygiene fixes. Two CRITICALs flagged in the QC report (auth-dead, SQL-injection) were independently verified as FALSE POSITIVES and intentionally NOT applied. ALLOW_INSECURE_OPEN_API was NOT set.

**Lane A — operator/FilePreview.tsx:** Replaced bare render-phase `fetchContent()` call with a `useEffect(() => { void fetchContent(); }, [src])` to comply with React's rules-of-hooks and stop the double-fetch on re-render.

**Lane B — ceo-board:**
- `ComparisonBar.tsx`: Added zero-division guards for `industryValue === 0` and `bestValue === 0` so the delta/percent calculations return 0 instead of NaN/Infinity on empty data.
- `LiveLogsSection.tsx`: Removed unused `useEffect, useState, useRef` imports that were flagged as dead code.
- `health/ScoreSparkline.tsx`: Deleted unused file (no import site found anywhere in the project).
- `health/index.ts`: Removed the corresponding re-export of the deleted `ScoreSparkline`.

**Lane C — lib:**
- `tasks.ts`: Escaped LIKE metacharacters (`%`, `_`, `\`) in the idempotency_key dedup query, using SQLite's `ESCAPE '\\'` clause, so keys containing those characters cannot false-match unrelated events.
- `openclaw/client.ts`: Replaced `any` parameter type on `generateEventId()` with a typed `GatewayFrame` interface covering only the fields the function reads.
- `routing/departments.config.ts`: Replaced two `any`-typed `db` parameters with `Database.Database` from `better-sqlite3`, removing the suppression comments.

## [v4.53.1] — 2026-06-27 — fix(fleet-heartbeat): jq/recreate-order bug in propagate-rescue-webhook.sh VPS secret block

Adds `fleet-heartbeat/scripts/propagate-rescue-webhook.sh` to origin/main for the first time and ships the jq/recreate-order bug fix flagged as a HANDOFF residual after the 2026-06-26 Rescue Rangers full-fleet propagation run.

**The bug (before):** In the VPS box loop the secret block (`RESCUE_RANGERS_WEBHOOK_SECRET` propagation) ran AFTER the `force-recreate` decision point. On any run where the webhook URL was already correct but the secret was new, `HOST_CHANGED` would be set to 1 AFTER the force-recreate check had already fired -- so the container was (re)created without the secret env var. The secret landed in the host `.env` file but the running container would not pick it up until the next manually triggered recreate.

**The fix (after):** The secret block now runs BEFORE the `force-recreate` check so `HOST_CHANGED` accumulates both the URL and secret deltas. A single `docker compose up -d --force-recreate` fires at the end and picks up both from the host `.env`.

**Additional fixes in the same file:**
- jq quoting hardened: replaced `docker exec sh -c "..."` (double-quoted body where shell expands `$k`/`$v` before jq sees them) with `docker exec -u node -e SK=... -e SV=... sh -c '...'` (single-quoted body; variables passed as env vars named SK/SV, never touched by the shell before jq).
- Idempotent "already correct" guard added for the secret in host `.env` to suppress spurious `HOST_CHANGED` on re-runs.
- `make_section()` upgraded to v2 structured-form: takes `box_name` and `box_type` args and emits a 9-field escalation payload (`person`, `clientName`, `agentName`, `boxName`, `boxType`, `openclawVersion`, `problem`, `alreadyTried`, `returnTo`) instead of the flat one-liner. Old v1 sections are detected and replaced in-place.
- An opted-out client's entry commented out (opted out of fleet-heartbeat propagation).
- Verify block extended: now also prints the `RESCUE_RANGERS_WEBHOOK_SECRET` presence and the `X-Rescue-Secret` header count in AGENTS.md.

**Files changed:** `fleet-heartbeat/scripts/propagate-rescue-webhook.sh` (new on origin/main).

## [v4.53.0] — 2026-06-26 — fix(dispatch): dispatch/route.ts null-fallback + dept-prefix resolver for Skill-6 Kanban flow

Closes the last gap in the Skill-6 (funnel/website) → Kanban demo path. Two files edited; no other files touched.

**dispatch/route.ts — `resolveSpecialistSessionKey` null-fallback (FG1):**
The route handler's copy of `resolveSpecialistSessionKey` still returned `agent:main:<session>` when no specialist runtime was found, sending every manual "Send to Agent" click to the CEO/Stefanie orchestrator whose prompt forbids building — she re-ingests the task (loop). Return type changed from `string` to `string | null`. On Attempt-1 (workspace-slug) the function now probes `~/.openclaw/agents/dept-<slug>/` BEFORE `~/.openclaw/agents/<slug>/` so live boxes (whose runtime dirs are `dept-funnels` / `dept-web-development`, not bare slugs) resolve on the first probe. When no runtime resolves, returns `null` — the caller writes a durable `routed_but_not_dispatched` row to `task_activities` + `events`, leaves the task in `backlog` (visible on board), and returns HTTP 202 `{success:false, held:true}` instead of feeding the CEO loop. Matches the hardened null-return that `task-dispatcher.ts` already had (v4.52.0, G8).

**task-dispatcher.ts — Attempt-1 dept-prefix probe (FG1):**
Same dept-prefix-first probe added to the `autoDispatchTask` path for consistency. The null-fallback refusal was already correct; only the probe order was wrong (bare-slug-first missed `dept-funnels` / `dept-web-development`).

Proof harness (5/5 pass, cited below):
- T1: funnels workspace slug → `agent:dept-funnels:<session>`
- T2: web-development workspace slug → `agent:dept-web-development:<session>`
- T3: unknown dept → null (`routed_but_not_dispatched`, NOT `agent:main`)
- T4: funnels role slug → `agent:dept-funnels:<session>`
- T5: bare 'funnels' dir absent → fix correctly probes dept-prefix first

## [v4.52.0] — 2026-06-26 — feat(workforce-engine): kanban push-through wiring + persona/SOP resolve + auth hardening

Command-Center half of the end-to-end AI-workforce ENGINE hardening (companion to openclaw-onboarding v14.3.3). Additive and separate from the concurrent ad-campaigns (v4.50.0), column-tooltips, and CEO/board workflows. Fixes Trevor's core worry that cards "sit at the very beginning and never move/assign/progress," plus persona/SOP wiring and a fail-closed auth posture.

KANBAN PUSH-THROUGH (G8) — the dispatcher (`task-dispatcher.ts`) is the ONLY correct authority for the `backlog → in_progress` flip (it flips AFTER `chat.send` succeeds). Two callers were pre-setting `in_progress`, so `autoDispatchTask` returned at GUARD-3 and the agent was never invoked. `webhooks/auto-route/route.ts` and `jobs/ceo-delegation-sweep.ts` now assign agent/department and LEAVE the card in backlog for the dispatcher to flip. NEW `jobs/backlog-redispatch-sweep.ts` (registered `*/2` in `jobs/scheduler.ts`) re-fires assigned-but-stuck backlog cards — self-limiting at the root (grace window, batch cap, attempt cap, master/CEO exclusion, kill switch; a card drops out the instant it flips to in_progress; the failing path returns before any `chat.send`/LLM call → no token burn, no self-resurrect).

STATUS ENUM LOCKSTEP (G9/G14) — `validation.ts` shared `TaskStatus` enum widened 5→10 to the canonical board manifest (`inbox/planning/assigned/testing/pending_dispatch` added), so legitimate moves (e.g. the To-Do drop `status='assigned'`) and `inbox`-on-create stop returning 400. `task-lifecycle.ts` `LifecycleState`/`LEGAL_TRANSITIONS` extended to all 10 (strictly more permissive; no previously-legal edge removed) and the over-claimed docstring corrected to reality (transition() is advisory, not the sole gate). LOCKSTEP RECONCILE: ad-campaign stage cards now validate against a dedicated 5-value `AdCardStatus` enum instead of the widened `TaskStatus`, keeping the schema in lockstep with `moveAdStage()` (prevents a board-only status reaching an ad card). DB↔ENUM LOCKSTEP (Migration 076 `widen_tasks_status_check_to_10_board_statuses`): the runtime `tasks.status` CHECK constraint (schema.ts) only permitted the original 5 statuses, so every write of a new status (`assigned/inbox/planning/pending_dispatch/testing`) threw `SQLITE_CONSTRAINT` at runtime even though `tsc` was clean — cards literally could not leave backlog. Migration 076 runs SQLite's official 12-step table rebuild (reads the live CREATE TABLE sql, replaces only the CHECK, dynamic column copy, `foreign_key_check` gate, `useOuterTransaction:false`) to widen the CHECK to all 10 statuses; idempotent (no-op if already widened), column-set-agnostic, all indexes/triggers preserved. Renumbered to `076` on rebase (upstream v4.51.0 claimed `075` for the renumbered `intelligent_model_selector`).

PERSONA/SOP RESOLVE (G10/G11) — `tasks.ts` resolves+pins the persona (`resolvePersonaAndPin`, bounded retry, no cron) BEFORE auto-dispatch reads `tasks.persona_id`, so the board persona == the runtime persona. `tasks/[id]/route.ts` auto-resolves a missing SOP/persona in-band before the Triad gate decides, so a human drag no longer 400-bounces when the pin merely hadn't landed. `intelligence-resolver.ts` Hop-10 sticky lookup now keys on `(department_id, task_category)` (ported `inferTaskCategory`, byte-verified vs the Python source) instead of department alone, killing the cross-category misroute. `task-dispatcher.ts` refuses the silent `agent:main` fallback (returns null) and emits a durable, queryable `routed_but_not_dispatched` hold instead of feeding the CEO re-ingest loop.

FLOOR SEED (G12) — `db/migrations.ts` + `role-library-import.ts` + `system/converge/route.ts` resolve departments.json / the departments tree from the active client company folder (`ZERO_HUMAN_COMPANY_DIR` / `BLACKCEO_COMMAND_CENTER_ROOT` / newest materialized ZHC company), so a freshly-built client seeds its OWN floor instead of a stale repo-committed default — with the historic workspace path preserved as a checked candidate + fallback (operator box not broken).

AUTH HARDEN (G15) — `middleware.ts` is now fail-closed: missing `MC_API_TOKEN` rejects external `/api/*` (503) and the two webhook routes require `WEBHOOK_SECRET`, instead of silently passing all traffic through; same-origin UI + CF-Access path preserved; reversible via the explicit `ALLOW_INSECURE_OPEN_API=true` bridge (loud startup error names the var). The health bypass is a SUBTREE match (`/api/health`, `/api/health/`, `/api/health/deep`, …) so Cloudflare/CI liveness probes stay reachable with no token set — the deep-health thin-probe must never be gated. The pipeline duck-e2e drives the board via unauthenticated server-to-server fetches and is not testing auth, so its test server alone sets the `ALLOW_INSECURE_OPEN_API` escape hatch (production never does).

STALE UI (G16) — the dead per-column '+' is wired to the working New-Task flow; Call Mode / Web Agent lose the misleading "Soon" badges (both routes are fully built).

Lockstep/CI: version rolled to v4.52.0 across all 4 markers (`scripts/bump-version.sh`); full-project `tsc --noEmit` = 0 errors. Rebased onto current main (3191a25c, v4.51.0). Operator must create annotated tag v4.52.0 at ship (version-consistency tag guard).

## [v4.51.0] — 2026-06-26 — fix(ci): full CI green — dup-072 migration, QC 5.2 self-match, duck-e2e AF-I14 trace

Repairs every red CI check on main (was: Duck pipeline e2e, QC Command Center, Port-pin and env-bleed guard) without weakening any guardrail.

Migration: the id `072` was registered twice — `blocked_fields_and_last_progress_at` and `intelligent_model_selector`. The runner builds its applied-set once before the loop, so on a FRESH DB the second `072` still passed the in-memory `applied.has()` guard, ran, and its `INSERT INTO _migrations(id,…)` hit the PRIMARY KEY → `UNIQUE constraint failed: _migrations.id`, crashing the duck-e2e seed before migration 074. In production whichever `072` ran first claimed the id, so the SECOND migration's schema change (`sops.model_pin` + backfills) never applied. Fix: renumbered the second `072` → `075` (next free id; current max was 074) so it actually applies on the next deploy. The up-migration is idempotent/guarded (`PRAGMA table_info` column guard before `ALTER TABLE … ADD COLUMN`; `CREATE INDEX IF NOT EXISTS` lifted out of the column guard so the partial index is ensured whether or not `model_pin` pre-existed; `agent_settings`/`tasks.model_id` backfills are no-ops when empty) and additive/reversible — manual down documented in the migration body (`DROP INDEX IF EXISTS idx_sops_model_pin; ALTER TABLE sops DROP COLUMN model_pin; DELETE FROM _migrations WHERE id='075';`). Existing prod DBs are untouched: the first `072` keeps its recorded id; the renamed migration runs once as `075`. Array position (and thus execution order) is unchanged. No destructive drops/renames.

QC gate: check `5.2` ("no 'anthropic/' provider id in src/lib") was a false-positive — it flagged its OWN negative-list declaration `const FORBIDDEN_PREFIXES = ['anthropic/', …]` in `model-selector.ts` (the guard that BANS Anthropic). Refined `scripts/qc-cc.sh` check 5.2 to exclude lines containing `FORBIDDEN_PREFIXES` (mirrors the `grep -v` style of check 5.1); real usage (e.g. `model: 'anthropic/claude-…'`) carries no `FORBIDDEN_PREFIXES` on the line and is still caught. Because both the "QC Command Center" job and the "Port-pin and env-bleed guard" job run the full `qc-cc.sh`, this single fix turns BOTH jobs green (the port/env-bleed section 11 self-tests already passed — there was no real port defect). `qc-cc.sh` now: PASS — 108 checks green, 0 warnings.

Duck e2e (test-only, AF-I14 unchanged): with the seed crash gone, two previously-masked subtests (g/h) surfaced. The AF-I14 KIE.ai image-path guardrail (broadened fleet-wide at v4.45.0) fail-closes any task whose title describes an image deliverable ("create a blue duck image") unless it finds an OpenClaw session exec trace proving `scripts/kie_generate.py` was used; the mock generator ships a PNG with no such trace → VIOLATION-C → status `backlog` (escalation to `blocked` is cron-deferred and the cron doesn't run under `next start`). Fix is in the test harness only: `tests/e2e/duck-test.ts` points the server's `HOME` at a throwaway dir and seeds a faithful KIE.ai session `.jsonl` trace (contains the taskId, a `python3 scripts/kie_generate.py` call and `api.kie.ai`, and no native `image_generate` tool block / dead endpoint) — the same artefact a real KIE-mode run records. AF-I14 then PASSES, the artifact-mode QC scores the PNG, and the task reaches a terminal state (`review`). The guardrail itself is untouched (fail-closed-on-no-trace is deliberate). Duck e2e now: 20/20 subtests pass.

Edited: `src/lib/db/migrations.ts` (072→075 renumber + hardened idempotency + documented down), `scripts/qc-cc.sh` (check 5.2 exclusion), `tests/e2e/duck-test.ts` (isolated HOME + seeded AF-I14 KIE.ai trace). Verification: `npm run build` exit 0; `npm run test:vitest` 73/73; `bash scripts/qc-cc.sh` exit 0 (108 green); duck-e2e 20/20.

## [v4.50.0] — 2026-06-26 — feat(board): ad-campaign cards (Skill 48 → Kanban)

Lands an external Facebook/Instagram ad assembly line on the existing board as a campaign + one card per stage, driven by the producer's receipt id. New `POST /api/ad-campaigns` (create-only, idempotent on `job_id`) creates a `campaigns` row + an epic parent card + one card per stage in `backlog`. New `PATCH /api/ad-campaigns/[id]` moves one stage card and is the SEPARATE status-update path: every move routes through the canonical `transition()` engine with `operatorOverride:true` (ad cards carry no assigned agent) so the two human pause points land on `review` without the QC auto-scorer skipping them. `GET /api/ad-campaigns?job_id=` polls a run. Auth mirrors `/api/tasks/ingest`: global Bearer middleware + per-route `x-webhook-signature` HMAC (both no-op when env unset). Creation uses a direct INSERT (mirrors `/api/campaigns`) — NOT `createTaskCore` — so no `autoDispatchTask()` agent runs fire per card; the OpenClaw `agent_id` is provenance only and is never written to the `assigned_agent_id` FK. Status map: created=backlog, working=in_progress, both human pauses=review, failure=blocked, approved=done.

Migration 074 `add_tasks_stage_slug_for_ad_campaigns` (additive + reversible): adds the single net-new column `tasks.stage_slug` (nullable; non-ad tasks stay NULL) plus a partial unique index `idx_tasks_campaign_stage` (one card per campaign_id+stage_slug) and `idx_tasks_stage_slug`. `campaign_id` and the blocked columns already exist (migrations 017 / 072). No destructive drops/renames. Manual rollback documented in the migration body (the runner has no auto-down; the change is purely additive so leaving it in place is safe): `DROP INDEX idx_tasks_campaign_stage; DROP INDEX idx_tasks_stage_slug; ALTER TABLE tasks DROP COLUMN stage_slug; DELETE FROM _migrations WHERE id='074';`. New files: `src/lib/ad-campaigns.ts`, `src/app/api/ad-campaigns/route.ts`, `src/app/api/ad-campaigns/[id]/route.ts`, `tests/unit/ad-campaigns.test.ts`. Edited: `validation.ts` (two zod schemas), `db/schema.ts` (stage_slug column), `db/migrations.ts` (id 074).

## [v4.49.0] — 2026-06-16 — feat(qc): AF-COVERAGE anti-compression gate

Blocks a deck whose slide count is materially below what its source warrants (auto-fail) UNLESS a client_requested_slide_cap is on record. Closes the hole where a rich transcript (~2800 lines, warranting ~30-62 slides) got silently compressed to a 12-slide summary. Pure checkCoverage(): FAIL if actual < 90% of slide_count_target with no cap; FAIL if large source (>1500 lines) + target <20 with no cap; PASS at >=90% of target or when an explicit client cap is honored; fail-closed on unknown. Emitted for deck tasks. Tests: AF-COVERAGE 14/14; AF-PIPELINE/SPELL/NUM/LANG/I14 regressions pass; tsc clean.


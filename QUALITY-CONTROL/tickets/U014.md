# U014 — Box health probe re-run + remote health

- **status**: fixed
- **branch**: `unit/U014-health-probe`
- **head_commit**: `b131e1af`
- **fixed_by**: Trevor Otts
- **fixed_at**: 2026-07-24

## Fixes applied

1. **Forward-reference crash**: Moved `gateway_to_http_base()`, `probe_remote_client()`, and `run_remote_health()` above the `REMOTE_MODE` gate (before line 99).

2. **EXIT_CODE unbound**: Initialized `EXIT_CODE=0` before the remote gate block.

3. **Residual quoting**: Removed `'"'"'` wrapping from 4 printf JSON format strings in `probe_remote_client()`. The `'"'"'` pattern creates unbalanced bash double-quote context which corrupts `%s` substitution.

4. **Awk quoting fix**: Removed `'"'"'` from awk program patterns in `probe_remote_client()` — prevented `$0` bash expansion in `NR>1{print prev} {prev=$0}`.

5. **Python inline fix**: Rewrote the tab-delimited client parsing in `run_remote_health()` from broken `'"'"'`-within-`-c "..."` quoting to clean single-quoted `-c '...'` with `<<<` here-string input.

6. **Behavioral tests**: Added `tests/unit/remote-health-probe.test.sh` with 11 tests:
   - T0: bash -n syntax check
   - T1: `--remote --dry-run --db-path <fixture-db>` prints client names, exits 0
   - T2: probe_body JSON parsing with known valid JSON reflects true/false correctly
   - T3: Mutation proof — deleting probe loop produces RED; restore produces GREEN
